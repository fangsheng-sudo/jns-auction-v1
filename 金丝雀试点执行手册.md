# 金丝雀试点执行手册（Runbook）

> 生成：2026-09-14 · **只读产出，未执行**
> 对象：JNS 拍卖系统四批合约
> 链：Jouleverse 主网 chainId **3666**｜RPC JNS DAO 官方 RPC（授权端点，部署时配置）

---

## 🔴 前置检查（不通过则不得开始）

**① 多签余额必须 ≥ 0.05 J**
```
cast balance 0x4eF599b6E39D950D6Ddbd830fF5f95e06770C1B3 --rpc-url $JNS_DAO_RPC_URL
```
- 实测（2026-09-14 22:15）：**0.320001 J** ✅ 充足（试点预算 ≈0.0366 J，余量 ~8.7×）
- ❌ 若 < 0.05 J ⇒ **首行红字：需人工充值**，不得继续

**② `<DEV_ADDR>` 已确定**（提醒服务开发运营方；须 ≠ 多签）
**③ DEPLOYER_ADDR 为持有私钥的 EOA**（非多签）
**④ 标的域名 `test-pilot-1.j` 未被铸造**
```
cast call 0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89 \
  "_nslookup(string)(uint256)" "test-pilot-1.j" --rpc-url $JNS_DAO_RPC_URL
# 期望输出 0
```

---

## 一｜部署与初始化

### 步骤 0-A｜部署五合约　执行者：**EOA 部署者**
```
DEPLOYER_ADDR=<持有私钥的EOA> \
forge script script/Deploy.s.sol:Deploy --broadcast \
  --rpc-url $JNS_DAO_RPC_URL
```
- **断言**：日志出现 5 条 `Deployed(what, addr)`；记下 5 个部署地址
- **验证**：`cast code <各地址> --rpc-url ...` 均非 `0x`
- ⚠️ 部署**不需要 owner 权限**；constructor 的 owner 已硬编码为多签
- ⚠️ **禁止**用多签作 broadcast 发送者（合约无私钥）

### 步骤 0-B｜多签初始化两条　执行者：**多签 2/3**
依据 `多签执行calldata.txt` §A：
1. `<FACTORY_ADDR>.setReviewer(0x5BF50F…B92076, true)`
2. `<REGISTRY_ADDR>.setRegistrar(<FACTORY_ADDR>, true)`
- **断言**：
  ```
  cast call <FACTORY_ADDR> "isReviewer(address)(bool)" 0x5BF50F…B92076 --rpc-url ...
  # 期望 true
  cast call <REGISTRY_ADDR> "isRegistrar(address)(bool)" <FACTORY_ADDR> --rpc-url ...
  # 期望 true
  ```
- **事件**：`ReviewerSet` / `RegistrarSet`

---

## 二｜一级拍卖全流程

### 步骤 1｜提交申请　执行者：**试点人 A（申请人）**
```
cast send <FACTORY_ADDR> "submitRequest(string,uint256,bytes32)" \
  "test-pilot-1.j" 1000000000000000000 0x00... \
  --private-key <A_KEY> --rpc-url $JNS_DAO_RPC_URL
```
- **断言**：event `RequestSubmitted(0, A, "test-pilot-1.j", 1e18, hash, ts)`
  ```
  cast call <FACTORY_ADDR> "getRequest(uint256)(address,string,uint256,bytes32,uint256,uint8,uint256)" 0 \
    --rpc-url ...    # status 应为 1 (Pending)
  ```

### 步骤 2｜审核　执行者：**J-0 个人地址（非申请人）**
```
# J-0 用【自己钱包】发起
cast send <FACTORY_ADDR> "approveRequest(uint256)" 0 --private-key <J0_KEY> --rpc-url ...
```
- **断言**：event `RequestApproved(0, J0, 1, ts)`；`getRequest(0).status == 2 (Approved)`
- **反证**：若 J-0 恰为申请人 ⇒ `FAC: reviewer cannot be applicant` revert（防自审）

### 步骤 3｜开拍（同 tx 拉起拍价 1 WJ）　执行者：**A**
```
# ① A 先授权工厂拉取起拍价
cast send 0x7fba9BB9…c6AD "approve(address,uint256)" <FACTORY_ADDR> 1000000000000000000 \
  --private-key <A_KEY> --rpc-url ...
# ② 创建拍卖（duration 168h）
cast send <FACTORY_ADDR> "createAuctionFromRequest(uint256,uint256)" 0 168 \
  --private-key <A_KEY> --rpc-url ...
```
- **断言**：
  - event `AuctionCreated(0, <AUCTION>, A, "test-pilot-1.j", 1e18, 168, ref, ts)`
  - ```
    cast call 0x7fba9BB9…c6AD "balanceOf(address)(uint256)" <AUCTION> --rpc-url ...
    # 期望 1000000000000000000（起拍价已入拍卖合约）
    cast call <AUCTION> "highestBidder()(address)" --rpc-url ...   # A
    cast call <AUCTION> "highestBid()(uint256)" --rpc-url ...     # 1e18
    ```
- **多签补登**：`<REGISTRY_ADDR>.setRegistrar(<AUCTION>, true)`（跨批手工项；每新建拍卖实例都要做）

### 步骤 4｜出价　执行者：**试点人 B**
```
# B 出 2 WJ（= 1 + ceil(5%) = 1 + 1）
cast send 0x7fba9BB9…c6AD "approve(address,uint256)" <AUCTION> 2000000000000000000 --private-key <B_KEY> --rpc-url ...
cast send <AUCTION> "bid(uint256)" 2000000000000000000 --private-key <B_KEY> --rpc-url ...
```
- **断言**：
  - event `Bid` + `Outbid(A, 1e18)`
  - `pendingReturns(A) == 1e18`｜`highestBidder == B`｜`highestBid == 2e18`
  - `WJ.balanceOf(<AUCTION>) == 3e18`
- **边界反证**：出 1 WJ（不满足最小加价）⇒ `EA: below minimum` revert

### 步骤 5｜结算　执行者：**任何人**
```
cast send <AUCTION> "settle()" --private-key <ANY_KEY> --rpc-url ...
# 需 block.timestamp >= endTime
```
- **断言**：event `Settled` + `MintRequested`；`escrow() == 1 (Held)`；合约仍持 **3 WJ（未放款）**

---

## 三｜铸造与放款

### 步骤 6｜铸造（**先铸给多签**）　执行者：**多签 2/3**
```
cast send 0xf8AbF36B…2035b89 "claim(string)" "test-pilot-1.j" --rpc-url ...   # 经多签流程
```
- **断言**：
  ```
  cast call 0xf8AbF36B…2035b89 "_nslookup(string)(uint256)" "test-pilot-1.j" --rpc-url ...
  # 期望 != 0（关键断言）
  cast call 0xf8AbF36B…2035b89 "ownerOf(uint256)(address)" <TID> --rpc-url ...
  # 期望 == 多签 0x4eF599…C1B3（claim 为 _safeMint(msg.sender)）
  ```
- **可选后续**：多签先 `unbind(TID)` 再 `transferFrom(多签 → B)` 把 NFT 交给赢家
  （JNS 硬约束：已绑定 token 不可转让）

### 步骤 7｜放款　执行者：**任何人**
```
cast send <AUCTION> "releaseToDAO()" --private-key <ANY_KEY> --rpc-url ...
```
- **断言**：
  - 前置 `ownerOf(TID) ∈ {B, 多签}`（白名单软校验）
  - event `WJReleasedToDAO(name, TID, curOwner, 2e18, 多签, ts)`
  - `escrow() == 2 (Released)`
  ```
  cast call 0x7fba9BB9…c6AD "balanceOf(address)(uint256)" 0x4eF599…C1B3 --rpc-url ...
  # 相比步骤 5 增加【恰好 2e18】（= highestBid，金额须等于成交价）
  ```

---

## 四｜验收断言清单（9 条）

| # | 断言 | 判据 / 方法 |
|---|---|---|
| 1 | `escrow` 由 **Held → Released** | `escrow()` == 2 |
| 2 | **款项到账多签**、金额 = 成交价 | `WJ.balanceOf(多签)` 增量 == `highestBid` |
| 3 | 申请人被超越后的资金 **可 pull 领回** | A 调 `withdrawRefund()`，`WJ.balanceOf(A)` +1e18 |
| 4 | 事件齐全 | `RequestSubmitted`/`RequestApproved`/`AuctionCreated`/`Bid`/`Outbid`/`Settled`/`MintRequested`/`WJReleasedToDAO` |
| 5 | **【关键】真实 JNS 上 `_nslookup(name) != 0` 且 `ownerOf` 校验通过** | 步骤 6 两条 `cast call` |
| 6 | 无异常 revert | 全步骤 tx status == 1 |
| 7 | 对账无尘差 | `WJ.balanceOf(<AUCTION>) == 0`（放款 + 退款完成后） |
| 8 | **WJ 零陷阱** | 无 `to=0/WJ` 分支触发；`Trapped` 事件 0 条 |
| 9 | **主网行为与本地一致** | 各步 revert 原因/事件与 `forge test`（**140/140**，历史基线 117）逐条吻合 |

---

## 五｜成本细项

| 项 | gas | 折算 J @2.5 gwei |
|---|---|---|
| 部署五合约 | 12,850,000 | 0.032125 J |
| 初始化 2 条 setter | 200,000 | 0.000500 J |
| developer 就位（propose+accept） | 100,000 | 0.000250 J |
| 全流程（申请→审核→开拍→出价→settle→claim→release） | 1,500,000 | 0.003750 J |
| **合计（不含标的款）** | **≈14,650,000** | **≈0.0366 J** |
| 标的款（WJ） | — | **1 WJ**（起拍价）；B 加价则 2 WJ，全数回流多签/申请人 |

> 实测 gasPrice = `0x9502f907` = **2.5 gwei**（2026-09-14）

---

## 六｜执行者一览

| 步骤 | 执行者 |
|---|---|
| 0-A 部署 | **EOA 部署者**（持私钥） |
| 0-B 初始化 | **多签 2/3** |
| 1 申请 | 试点人 A |
| 2 审核 | **J-0 个人地址** |
| 3 开拍 | 试点人 A（+ 多签补 setRegistrar） |
| 4 出价 | 试点人 B |
| 5 settle | **任何人** |
| 6 claim 铸造 | **多签 2/3** |
| 7 releaseToDAO | **任何人** |
