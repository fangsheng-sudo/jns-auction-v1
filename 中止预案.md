# 中止预案（Abort Plan）

> 生成：2026-09-14 · **只读产出，未执行**
> 适用：JNS 拍卖系统金丝雀试点（及此后公开拍卖）
> 铁律：**试点失败 → 停机报告，不得「重试绕过」**

---

## 一｜失败处置总则

1. **立即停机**：停止推进后续步骤，不执行任何 `--broadcast`。
2. **保留证据**：记录 tx hash、revert 原因、事件日志、块高；落盘存档。
3. **停机报告**：向 J-53 报告「哪一步、什么现象、链上证据、我的判断」。
4. **禁止绕过**：不得以「再试一次」「换个参数试试」等方式继续 —— 合约**不可升级**，每一次广播都是不可逆的主网动作。
5. **复现优先**：先在本地用 `forge test` 复现同现象，定位是**设计缺陷**还是**环境差异**，再决定是否重跑。

---

## 二｜可修复重来 vs 不可逆（按「是否已广播」划分）

### ✅ 可修复重来（**未广播**前，零成本）

| 项 | 说明 |
|---|---|
| 参数填错（`<DEV_ADDR>`、受益地址、起拍价） | 干跑阶段任意改 |
| constructor 参数错误 | 改 `script/Deploy.s.sol` 后重跑干跑 |
| calldata 错误/缺失 setter | 改 `SetterCalldata.s.sol` / calldata 包 |
| 本地测试失败 | 改代码或测试后重跑，无链上后果 |
| runbook 步骤/断言写错 | 直接改文档 |
| 多签余额不足 | 充值后再执行 |

> 判据：**只要没有 `--broadcast` 到主网，一切可改，成本 = 0。**

### ⛔ 一旦广播即不可逆

| 动作 | 不可逆后果 |
|---|---|
| **部署五合约** | 合约代码上链、地址永久；**无 proxy、无 UUPS、不可升级** ⇒ 代码逻辑**无法修改** |
| **多签执行 A 段 2 条** | `setReviewer`/`setRegistrar` 上链；可撤销（`ok=false`）但**留下公开记录** |
| **提交申请 / 审核** | 请求记录永久上链（含 payloadHash） |
| **创建拍卖** | 部署一个拍卖实例（不可升级）+ **拉取申请人起拍价 WJ** |
| **出价** | WJ 转入合约；被超越者退款仅能**自己 pull** |
| **settle** | 进入 Held 托管态；`highestBidder`/金额锁定 |
| **`JNS.claim` 铸造** | **JNS 域名永久铸出**（JNS 无过期、无销毁、不可升级）；**不可撤回** |
| **`unbind` + NFT 转移** | CSBT 绑定被解除，NFT 归属永久变更 |
| **`releaseToDAO`** | **WJ 永久转出**至多签，`escrow` = Released（与退款出口互斥，**不可回滚**） |

> ⚠️ **核心结论**：**广播后发现问题，只能「重新部署 + 地址迁移」，旧合约无法修补。**
> 具体含义：
> - 旧合约若有 bug ⇒ 只能部署新版本，**旧地址作废**；
> - 若旧合约**仍持有 WJ/NFT**（如卡在 Held 或 pendingReturns）⇒ 需**逐个用户 pull 领回**，或走治理/多签转移；
> - **已铸出的 JNS 域名无法回收**（JNS 无销毁函数）⇒ 只能用二级市场/多签转回转出；
> - 所有对外公布的地址（文档、社群、banner）都要**同步更新**，否则用户会打到旧合约。

---

## 三｜分阶段中止点（决策门）

| 门 | 时点 | 若不通过 |
|---|---|---|
| **G0** | 干跑 + 本地测试 | 改代码/测试，**不上链** |
| **G1** | 部署完成、**未执行初始化** | 放弃该批地址，重新部署（成本 ≈0.032 J） |
| **G2** | 初始化完成、**未提交申请** | **可安全停**：合约空转，未触碰用户资金 |
| **G3** | 拍卖已创建、**未有人出价** | `emergencyCancelAuction` 退回申请人起拍价（pull 领回） |
| **G4** | 有人出价、**未 settle** | 等待到期后 settle→超时退款，或治理决策；出价者资金可 pull |
| **G5** | 已 settle（Held）、**未铸造** | ① 若域名未铸 ⇒ 45 天超时全额退赢家；② 若已铸 ⇒ 只能走 `releaseToDAO` |
| **G6** | 已 `releaseToDAO`（Released） | **终局**，链上无回滚手段；只能链下协商 |

> 实务建议：金丝雀试点**在 G2 之后仍可全身而退**；真正的「不可逆悬崖」在 **G5 的 `claim`（铸造域名）与 `releaseToDAO`（转出 WJ）**。

---

## 四｜最坏情形与兜底

| 最坏情形 | 兜底路径 |
|---|---|
| 部署后发现合约逻辑 bug | **地址迁移**：重新部署 + `setRegistrar(新factory)`；旧合约如有资金，用户自行 pull |
| 多签在**铸造前**失效 | 域名铸不出 ⇒ 45 天超时**全额退赢家**（出口②）；系统需治理介入恢复多签 |
| 多签**铸而不转** | 赢家**无链上救济**（见风险清单 R9）；依赖多签诚信 → 治理特别决议 |
| 铸给了无关第三方 | `releaseToDAO` **白名单软校验 revert**，资金停 Held；治理纠正后重触发 |
| 试点未通过全部 9 条断言 | 停机报告；本地复现 → 修 → **重新试点**（旧地址废弃） |
| CGC 对收入归属有异议 | 归属参数可改（`serviceFeeRecipient`/`auctionBeneficiary` setter），**但已成交的历史分账不可追溯** |

---

## 五｜中止后必做清单

- [ ] 记录并公开失败原因（对已参与者透明）
- [ ] 若合约持有资金：公告 pull 领回入口与截止期
- [ ] 若已铸域名：与赢家协商 `transferFrom` 路径
- [ ] 更新所有对外地址引用（文档/社群/banner）
- [ ] 本地复现 + 修复 + 全量测试 → 再评估是否重启试点
- [ ] 回写风险清单与设计说明
