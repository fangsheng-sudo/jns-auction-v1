# 域名系统安全自查报告

> 生成：2026-09-16｜对象：JNS 拍卖系统四批合约（EnglishAuction + JNSAuctionFactory / SecondaryMarket + ClaimRegistry / SubnameRegistry / NotificationService）
> 方式：**只读代码审计**（未改任何合约、未部署、未动多签）
> 结论：**无 P0、无 P1**；P2 共 6 项（见 §三），均为已披露/已标注/增强类，**不阻塞提交**
> 归属：本地底账 + 个人仓库归档；**不写入 jips#9**（等审核方（J-25）有回音时作补充材料）

---

## 一｜Q1~Q6 逐项结论

| # | 问题 | 结论 | 依据（合约:行号） | 等级 |
|---|---|---|---|---|
| **Q1** | 二级市场中间态（unbind → 托管 两笔交易之间） | **归属=卖家本人；第三方不可抢占**；真实风险是「卖家自己抢转」，契约接受该风险 | `设计说明.md:329-341`；`SecondaryMarket.sol:114-116`（list 校验 `ownerOf==本合约`）；全库 `contracts/` 无 `unbind` 调用，`IJNS` 未声明 `unbind`（`deps/Deps.sol:120`） | P2（已披露，非缺陷） |
| **Q2** | 退款 pull 还是 push；push 被拒是否阻塞结束 | **pull（用户自取）**，恶意拒收不阻塞 | `EnglishAuction.sol:163`（出价被超越→挂台账）、`:205-211`（`withdrawRefund`）；`SecondaryMarket.sol:147`、`:223-229` | 已处理 ✅ |
| **Q3** | WJ 假设；转账损耗/税费/非标准返回 | **非标准返回已兼容**（无返回值代币放行）；**转账损耗/税费无防护** | 兼容：`deps/Deps.sol` `SafeERC20._callOptionalReturn`（`ret.length==0 ⇒ true`）、`trySafeTransferFrom`；无防护：先 transfer 再记账、无余额差校验（`EA:159-163`、`SM:143-148`） | P2 |
| **Q4** | 最小加价幅度；小额反复抬价 griefing | **`ceil(最高价×5%)`，下限 1 WJ**；无「0 加价」，griefing 有界 | `EnglishAuction.sol:136-143`、`SecondaryMarket.sol:85-91`；延时上限硬封顶 `EA:171-176`、`SM:153-158`（`startTime+192h`） | P2（参数权衡，关联待裁定 #12） |
| **Q5** | 提醒服务 v1 不启用，函数是否有访问控制 | **有 `onlyDeveloper`；外部无法误调用、不扣费** | `NotificationService.sol:175`（`settleBatch` `onlyDeveloper`）、`:63`（`perUseFee=0`）、`:237`（`zero amount` 跳过）；用户自操作函数 `:128-156`；setter `:286/:293/:301` | 已处理 ✅ |
| **Q6** | 是否有 pause/中止；上线后缺陷止损手段 | **全库无 pause/Pausable**；分合约有「叫停」出口，但 **SM 缺治理级中止** | 无 pause：`grep pause|Pausable|circuit` 零命中；出口：`EA:304` `emergencyCancel`(onlyOwner)、`JNSAuctionFactory.sol:260` 转发、`ClaimRegistry.sol:106` `cancelTask`、NS 可 `setPerUseFee(0)`/`setFeeCap`/用户 `revokeAuthorization`/`withdrawBalance`、`JNSAuctionFactory.sol:277` 转黑洞冻结 | P2 |

---

## 二｜4 项补充追问

### 2.1 到期后取回路径（最高优先）

**结论：双出口齐备，无 NFT 或资金永久锁定 ⇒ 非 P0/P1。**

| 场景 | 卖家取回 | 最高价者 settle | 依据 |
|---|---|---|---|
| **EnglishAuction 到期后** | **有** —— `settle()` permissionless（`EA:214`）；申请人兜底中标走 `claimTimeoutRefund`（仅赢家，`EA:280`） | **有** —— `settle()` 无权限限制（`EA:214`），到期 `anyone` 可调 | `EA:214`、`EA:243`（`releaseToDAO` 任何人）、`EA:280` |
| **SecondaryMarket 到期后** | **有** —— `settle()` permissionless（`SM:173`）；无人出价分支 `SM:179-186` ⇒ `NFT.transferFrom(合约→seller)` | **有** —— `settle()` 无权限限制（`SM:173`）；有出价分支 `SM:188-206` ⇒ NFT 直转 winner + WJ 分账 | `SM:173`、`SM:179-186`、`SM:188-206` |

**澄清**：`cancelListing`（`SM:208`）是「**未到期 + 无人加价**」的卖家主动下架，**不是**到期版；到期一律走 `settle`（已足够，无需 reclaim/withdrawUnsold）。

### 2.2 原子性（unbind + transferFrom）

**结论：两个独立 tx，不可合并——JNS 硬约束的必然。**

- 全库 `contracts/` **无任何 `unbind` 调用**（仅注释）；`IJNS`（`deps/Deps.sol:120`）**未声明 `unbind`** ⇒ 合约无能力代调；测试中 `jns.unbind()` 均由卖家 EOA 自发（如 `test/TestEF.t.sol:14`）。
- 硬约束：① `unbind` 要求 `ownerOf == msg.sender`（合约代调必失败）；② `transferFrom/safeTransferFrom` 全带 `requireUnbound`（未解绑转不动）⇒ **必然两笔 tx**（`设计说明.md:329-341`）。
- 中间态：归属=**卖家**（unbind 只清绑定、不改所有权）；**无第三方抢占可能**（转移需 owner 授权）；风险=卖家自抢转（违约），缓解=`list()` 校验 `ownerOf==本合约`（`SM:114-116`）+ 卖家自担 + 仲裁。
- ⚠️ 材料表述**不得**写成「原子完成」（与事实相反）；现「必然两笔 + 抢转窗口 + 缓解」表述**准确，保持**。

### 2.3 重入与不变量

- **重入：已处理 ✅** —— `withdrawRefund`（`EA:205-211`）读→先清零→再转账，严格 CEI，带 `nonReentrant`；SM 同构（`SM:223-229`）。专门测试：`test/TestSupplement.t.sol:158-167`（回调内重入 → 额度已清），`:175-180`（全流程零回调）。
- **不变量：无 `invariant`/`fuzz` 覆盖（P2，增强项）** —— `grep invariant|testFuzz` 零命中；现仅有弱断言 `require(wj.balanceOf(a)==0)`（`TestSupplement.t.sol` 单场景）。**代码上不变量成立**：EA 负债 = `Σ pendingReturns + (escrow==Held ? highestBid : 0)`；出入逐笔对平（`EA:159` 入、`EA:209/262/298` 出）。建议后补 1~2 条 invariant 测试（见 §三、§四）。

### 2.4 材料标注（WJ 硬前提）

- **已落地**：`README.md`「## ⚠️ 声明」段新增独立「硬前提」条目（commit `ee271621`）。
- 原文：**硬前提**：本系统假设 WJ 为标准 ERC20（18 位整数、无转账税、无 rebasing、无回调）；WJ 若变更特性，须重新审计。
- 备选位置（未用）：`设计说明.md` 开篇「§0 前置与硬前提」。

---

## 三｜已排除的三大经典风险

| 经典风险 | 结论 | 依据 |
|---|---|---|
| **退款 push 被拒 ⇒ 拍卖无法结束** | **已根除（pull 机制）** | 出价被超越仅挂 `pendingReturns` 台账（`EA:163`/`SM:147`），用户自取 `withdrawRefund`（`EA:205`/`SM:223`）；`settle` 的 push 仅面向固定收款方，WJ 为标准 ERC20 转账、**不回调收款方**，无法被拒收 |
| **重入** | **已处理（CEI + nonReentrant + 专门测试）** | `EA:205-211`、`SM:223-229`（先清零后转账 + `nonReentrant`）；`TestSupplement.t.sol:158-167` 回调内重入状态一致 |
| **到期取回无路 ⇒ 永久锁定** | **已排除（settle 无权限限制）** | `EA:214`、`SM:173` 均 permissionless；SM 无人出价分支退回卖家（`SM:179-186`） |

---

## 四｜P2 清单与处置建议

| # | P2 项 | 现状 | 处置建议 |
|---|---|---|---|
| P2-1 | **WJ 标准 ERC20 硬前提** | ✅ **已标注**（README 硬前提条目） | 保持；WJ 若变更特性须重新审计 |
| P2-2 | **owner 紧急中止权（`emergencyCancel`）** | ✅ **已披露**（设计说明 §2.5「权限披露（主动）」） | 保持；审核方（J-25）对权限敏感，主动披露优于被问出 |
| P2-3 | **无 `invariant`/`fuzz` 覆盖** | ⏳ 增强项（暂缓） | 后补 1~2 条（随机 bid + 随机 withdraw，每步断言 `wj.balanceOf(合约) >= Σ负债`）；定位「**提交后增强**」，**不宣称同一批**；总条数 140 → 140+N 须回报具体数 |
| P2-4 | **全库无 pause / SM 缺治理级中止** | 现状（**未自行添加**） | **提请审核方（J-25）裁定**：是否需增加治理级中止（如 SM listing 冻结/撤回）？合约不可升级 ⇒ 事后无法补 |
| P2-5 | **三上链合约尺寸**：EnglishAuction 11,827 / EnglishAuctionDeployer 14,783 / JNSAuctionFactory 12,220（均 < 24,576 B） | 现状 | contracts/ 下三个上链合约均须 < 24,576 B；`script/Deploy.s.sol` 为部署脚本、内嵌全部合约字节码、恒超 24KB 且不上链，不计入判定。今后任何改动**先算尺寸**（`forge build --sizes`） |
| P2-6 | **`Deploy` 恒超 EIP-170（已知，不上链）** | 现状（**无害**） | `script/Deploy.s.sol` 为部署脚本、内嵌全部合约字节码、恒超 24KB 且不上链，不计入判定（旧「sizes 必须 exit 0」门槛永远无法满足，属伪失败）。脚本不上链（`forge script --broadcast` 只广播业务合约，anvil 实证 5 笔均无 `Deploy`） |

---

## 五｜附：项目风险登记更新

以下三条已同步登记至项目风险登记（§七 其他残余风险）：
1. **owner emergencyCancel**（权限集中，已主动披露）；
2. **`JNSAuctionFactory` 658 B 余量**（今后改动须先算尺寸）；
3. **WJ 标准 ERC20 硬前提**（已标注，特性变更须重新审计）。

注：项目风险登记中已存在的 R-4（`emergencyCancel` 权限集中）、X-1（不可升级+无测试网）、X-8（lint note 181）与本报告一致，未重复新增。

---

## 六｜总评

| 维度 | 结论 |
|---|---|
| P0 | **0** |
| P1 | **0** |
| P2 | **6**（§四），均为已标注/已披露/增强/待裁定类，**不阻塞提交** |
| 三大经典风险 | **全部排除**（pull 退款、重入、到期取回） |
| 建议 | 保持现有材料表述（含「必然两笔 + 抢转窗口」）；P2-3 视你决定后补；P2-4 提请审核方（J-25）裁定 |
