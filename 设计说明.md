# JNS 域名拍卖系统 · 整体设计说明（最终整合版）

> 2026-09-14｜**本阶段只交设计，不写合约代码**
> **本文件取代此前全部零散指令与《JNS拍卖系统-需求确认与骨架.md》v1 骨架。**
> 表述规范：链上事实标【链上查明】+ 出处；业务裁定标【J-53 裁定】。
> 全程只读：未 commit、未 push、未部署。

---

## 零｜地址与参数独立复核

### 0.1 EIP-55 校验和 —— 用自实现 keccak256 独立验算

工具：`js-sha3` 的 `keccak256` + 自写 EIP-55 算法（**不复用上一轮任何结论**）。

| 候选地址 | 校验 | canonical 形式 |
|---|---|---|
| `0x7fba9BB966189Db8C4fE33B7bf67Bfa24203c6AD` | **PASS** ✅ | 同左 |
| `0x7fba9BB966189Db8C4fE33B7bf67Bfa24203C6AD`（大写 C） | **FAIL** ❌ | `…24203c6AD` |
| `0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89` | **PASS** ✅ | 同左 |

⇒ **独立确认**：WJ 地址尾部为 **`24203c6AD`（小写 c）**；上一轮我把它写成大写 `C` 是我的笔误。

### 0.2 链上独立复核（区块高度 **8291413**）

| 调用 | WJ（`0x7fba…c6AD`） | JNS（`0xf8Ab…5b89`） |
|---|---|---|
| `name()` | **`Wrapped Joule`** | **`J Name Service`** |
| `symbol()` | **`WJ`** | **`JNS`** |
| `decimals()` | `0x…12` = **18** | — |
| `totalSupply()` | — | **822** |
| `eth_getCode` | 有代码 | 有代码 |

### 0.3 两极相似地址的显式区分（T2 要求）

```
WJ  : 0x7fba9BB966189Db8C4fE33B7bf67Bfa24203c6AD
              ↑ 尾部 ……24203c6AD（小写 c）—— 代币，18 位，可自由转账
JNS : 0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89
              ↑ 尾部 ……2035b89（无 2420…c6AD 段）—— NFT，有 owner，不可升级
```
> **两个常量在源码中必须相邻并各带一行醒目注释**，禁止只写裸地址。

### 0.4 说明：一处口径修正

首次统计 code size 时曾写 `len(result)//2`，**把 `0x` 前缀算作 1 字节**，得 WJ=8634B；
正确应为 `(len(result)-2)/2` = **8633B**（与上一轮一致）。JNS 同理：**11372B**。
⇒ 已修正：链上返回值的 hex 前缀必须显式剥离，不可用 `len//2`。

---

## 一｜范围与批次（【J-53 裁定】）

| 批次 | 模块 | 状态 |
|---|---|---|
| **第 1 批** | `EnglishAuction` + `JNSAuctionFactory` | 本设计覆盖；**待放行代码** |
| **第 2 批** | `SecondaryMarket` + `ClaimRegistry` | 本设计覆盖 |
| **第 3 批** | `SubnameRegistry` | 本设计覆盖 |
| **第 4 批** | `NotificationService`（仅链上） | 本设计覆盖 |

| 场景 | 决定 |
|---|---|
| ① 一级拍卖（人工审核 + 链上留痕 + 168h） | ✅ 做 |
| ③ 二级市场（持有者挂拍，收入归卖家） | ✅ 做 |
| ④ 子域名/子子域名（**铸造为 NFT**） | ✅ 做 |
| 提醒服务（**仅链上部分**） | ✅ 做 |
| ② 过期回收 | ❌ **不做**（【链上查明】JNS 无 expire 字段） |

> **关于上一批已交付代码**：`contracts/{EnglishAuction,JNSAuctionFactory}.sol`（2026-09-14 16:00 版）
> 是**上一轮零散指令**下的产物，**已被本设计取代**（新指令新增：审核留痕、起拍价=申请人首出、整数规则、
> 紧急叫停、developer 角色、分模块双参数等）。**待你放行后将按本设计重写**，旧稿仅作留档。
> 已在该目录放 `SUPERSEDED.md` 标注。

---

## 二｜第 1 批｜一级拍卖设计

### 2.1 审核留痕（链上锚定，弃用 GitHub issue）

**数据模型（工厂合约内）**

| 字段 | 含义 |
|---|---|
| `requestId` | 自增序号 |
| `applicant` | 申请人地址（**用自己钱包**提交，不可抵赖） |
| `name` | 域名（全串，匹配 JNS `_nslookup` 语义） |
| `startingPrice` | 起拍价（WJ，整数） |
| `payloadHash` | 申请材料 hash（链下材料 → 链上锚点） |
| `createdAt` | 提交时间戳 |
| `reviewers[]` | **审核人地址数组**（每个审核人用**自己钱包**签名调用） |
| `approvedAt` | 达标通过时间戳 |
| `status` | `Pending / Approved / Created / Rejected / Cancelled` |

**流程**
1. `submitRequest(name, startingPrice, payloadHash)` —— 申请人本人调用，`status=Pending`。
2. `approveRequest(requestId)` —— **审核人用自己钱包**调用；记录 `reviewers` + 时间戳；去重（同一地址重复调用 revert）。
3. 当 `reviewers.length >= signersRequired` → `status=Approved`，emit 审核通过事件。
4. `createAuctionFromRequest(requestId, durationHours)` —— 达标后创建 `EnglishAuction` 实例；**同 tx 从申请人拉取 `startingPrice` WJ 直入该实例**。

**"责任具体到人"的实现**：审核人**地址**写在链上 ⇒ 与 `core-contributors.md` 的 Core ID 可映射，
不可删改、不可抵赖（对比 GitHub issue 可编辑/可删除）。

**★ 会签配置（Q1）**
- `signersRequired` 为**可配置**参数（默认 **1**）。
- 会签模式下 `approveRequest` 需 **N 个不同地址**各调一次，`approvedBy[requestId][addr]` 去重。

**资金拉取路径（设计要点）**
- 申请人 **approve 给工厂**（一次性），工厂 `safeTransferFrom(applicant → 新拍卖实例, startingPrice)`。
- **只发生 1 次 WJ transfer**，工厂**不持有资金**（无托管风险）。
- 备选：创建后再 approve 给实例地址 ⇒ 需两笔交易，不推荐。

---

### 2.2 起拍价 = 申请人首次出价（无流拍）

| 项 | 设计 |
|---|---|
| 创建时 | 申请人锁定 `startingPrice` 的 WJ（见 2.1 资金路径） |
| 初始状态 | `highestBidder = applicant`、`highestBid = startingPrice` |
| 无人加价 | `settle()` 时**申请人中标**，款项按 §6 路由 ⇒ **「流拍」概念消失** |
| 有人加价 | 申请人被超越 ⇒ 其锁定资金进入 `pendingReturns[applicant]`，**pull 自领** |
| `reservePrice` 参数 | **取消**（由 `startingPrice` 取代） |

**⚠️ 经济含义须点明**：此设计下**申请人承担"必付"义务**——只要无人加价，申请人必须按起拍价买入。
这是「消除流拍」的必然代价，属**设计意图**，但应在产品文案中显著提示申请人。

---

### 2.3 整数规则（仅约束拍卖出价/起拍价）

**规则**
- 起拍价 **≥ 1 WJ 且必须为整数 WJ**（即 `% 1e18 == 0`）
- 出价金额**必须为整数 WJ**
- 最小加价 = **`ceil(最高价 × 5%)`，向上取整到整数 WJ**；结果 < 1 WJ 时取 **1 WJ**

**★ 必须 ceil，不得 floor / 四舍五入**（否则低价区 5% 被算成 0，规则失效）。

**算法（按你给的伪码，逐行核对无误）**
```
inc = highestBid * 5 / 100          // 整数除法（wei）
r   = inc % 1e18
if (r != 0) inc = (inc / 1e18 + 1) * 1e18   // 向上取整到整数 WJ
if (inc < 1e18) inc = 1e18                  // 下限 1 WJ
```

**边界用例逐条验算（全部通过）**

| 用例 | 验算 | 期望 | 结果 |
|---|---|---|---|
| 0.9 WJ 起拍 | 0.9e18 % 1e18 = 0.9e18 ≠ 0 | **revert** | ✅ |
| 1.5 WJ 起拍 | 同上 | **revert** | ✅ |
| 非整数出价 | `% 1e18 ≠ 0` | **revert** | ✅ |
| 最高价 1 → 出价 1 | min = 2 WJ | **revert** | ✅ |
| 1 → 2 | inc=(5e18/100)=0.05e18 → 取整 → **1 WJ** ⇒ min=2 | **通过** | ✅ |
| 20 → 21 | inc=20×5%=1.0 WJ 整除 ⇒ min=21 | **通过** | ✅ |
| 21 → 22 | inc=1.05 → 取整 **2 WJ** ⇒ min=23 | **revert** | ✅ |
| 21 → 23 | 同上 min=23 | **通过** | ✅ |
| 100 → 104 | inc=5 WJ 整除 ⇒ min=105 | **revert** | ✅ |
| 100 → 105 | 同上 | **通过** | ✅ |

**溢出**：`highestBid * 5` 对 uint256 安全（实测 WJ 总量 ~5.4e26，5 倍仍远低于 2^256）。

**断言（写入合约）**：`startingPrice >= 1e18`；所有金额 `% 1e18 == 0`。

---

### 2.4 时间与防狙击

- 时长 **168h**（默认，可传参）
- 末 **10 分钟**内出价 → `endTime += 10min`
- 总时长上限 **192h** ⇒ **到点强制截止**（168h + 24h 恰为 192h，自洽）
- 时间基准 **`block.timestamp`**（【链上查明】出块 15s）
- 事件：`Extended(newEndTime)`

---

### 2.5 紧急叫停

`emergencyCancel(string reason)` —— **onlyOwner**
- 仅 `!settled` 时可用
- 取消后**所有出价 pull 退款**（含申请人的 `startingPrice`）
- `reason` **链上留痕**（`string` 存储 + 事件）
- 实现要点：置 `cancelled=true`；把 `highestBid` 归入 `pendingReturns[highestBidder]` 后清空，保证申请人与后续出价者均可自领

> **权限披露（主动）**：EnglishAuction 保留 owner 紧急中止权（`emergencyCancel`，EA:304）：仅适用于未结算拍卖，中止后全额挂 pull 退款。

---

### 2.6 超时退款与 ClaimRegistry（第 2 批）

| 项 | 设计 |
|---|---|
| 触发条件 | `MintRequested` 发出后 **45 天**仍未铸造 |
| 动作 | 赢家**主动**调 `claimTimeoutRefund()` ⇒ **全额退 WJ**（**非自动退**，正常流程零干扰） |
| 退款后 | 该拍卖 **`escrow = Refunded`**（**非 `cancelled`**）；名字【不】释放（已铸造则占死，见 §2.6.1） |
| 合约名 | **`ClaimRegistry`** —— **不承担 JNS owner**，只**记录待铸造任务** |
| 开关 | **可配置**（不需要时可关闭） |

**任务模型**：`{ auction, name, winner, amountWJ, reviewRef, requestedAt, minted, refunded }`

#### ⚠️ 与 3.2 的口径冲突（**已裁定，见下**） → 见 §2.6.1

3.2 写「无人加价 → settle 时…**款项进 JNS DAO**」（结算即付款）；
3.6 又要求「45 天未铸造 → **赢家全额退 WJ**」（钱须还在）。

### 2.6.1 【J-53 裁定】铸造前资金托管路径（2026-09-14 18:17，关闭 D-1）

**settle 后赢家款项【暂存拍卖合约】，不立即转 DAO。**

| 项 | 设计 |
|---|---|
| 暂存位置 | **拍卖合约**（`EnglishAuction` 内 `escrowedWJ` 余额） |
| 出口① 正常 | `releaseToDAO()` —— **任何人可触发**（无权限门槛）；**收款地址 = 创建时快照的 `auctionBeneficiary`**，**不可指定**；**前置：链上可验证的铸造事实**（见下 M1） |
| 出口② 超时 | `claimTimeoutRefund()` —— **赢家本人**触发；**两分支**：①未铸造→放行退款；②赢家持有→拒（改走 releaseToDAO）；**③其余（含治理方托管即压在多签、无关第三方、NFT 误入本合约）→放行退款**；**前置 `≥ requestedAt + 45 days`** |
| 互斥性 | **状态标志 `EscrowState { Held, Released, Refunded }` 保证二选一**；任一出口执行后置位，另一出口 revert |
| 暂存期限制 | **任何人都不得挪用**（合约内无其它转出路径；`emergencyCancel` 对已 settle 的拍卖不可用） |
| 事件 | `WJReleasedToDAO(auction,name,amount,beneficiary,ts)`；`TimeoutRefunded(auction,name,winner,amount,ts)` |

> **✅ 由此，45 天退款来源成立**（钱一直在合约里，既没给 DAO 也没被任何人动过）。
> **注意**：45 天计时起点 = **`MintRequested` 发出时刻**（`requestedAt`），非 settle 时刻。

### 2.6.2 【J-53 裁定·M1】markMinted 硬化（2026-09-14 20:12）

**实测发现**（JNS DAO 官方 RPC（授权端点，部署时配置），JNS `0xf8AbF36B…2035b89`）：

| 接口 | 选择器 | 实测结果 |
|---|---|---|
| **`_nslookup(string)`** | `0x54cf09e1` | ✅ **存在且公开**：`"bit"`→**15**（0x0f）、`"evan"`→**148**（0x94）→ **name→tokenId** |
| **`name2addr(string)`** | `0x43388825` | ✅ **存在**：`"bit"`→`0x5bf50f…b92076`（绑定地址） |
| `ownerOf(uint256)` | `0x6352211e` | ✅ 标准 ERC-721：`ownerOf(1)`→`0x095b03…`、`ownerOf(2)`→`0x44ba49…` |
| `getTokenId` / `nameToTokenId` | — | ❌ 不存在（revert） |

**⇒ 采用【方案 A】：`releaseToDAO` 直接链上校验，不依赖 `markMinted`。**

```
tokenId = JNS._nslookup(name)          // 该 name 对应的 tokenId
require(tokenId != 0, "name not minted");
require(JNS.ownerOf(tokenId) == winner, "winner mismatch");
```

- **无人工标记、无 owner 手动置位** ⇒ 「链上可验证的铸造事实」成立。
- `markMinted` **取消**（或仅作只读缓存事件，不参与放款判定）。
- 附带记录 **`tokenId`** 于事件 `WJReleasedToDAO(…, tokenId, …)`。

**✅ 已实测：不存在的 name 返回 `0`，不 revert**⇒ `require(tokenId != 0)` 为**安全判据，无需 try/catch**。
依据：`_nslookup` 是 `mapping(string=>uint256)` 的 **public 自动 getter**，缺失键**结构性返回默认值 0**，不可能 revert。
实测：`_nslookup('definitely-not-exists-xyz')`→0、`_nslookup('')`→0、`_nslookup('zzzz9999')`→0；对照 `_nslookup('bit')`→15、`_nslookup('a')`→41。
**哨兵值安全**：JNS `claim` 用 `tokenId = totalSupply() + 1` 从 **1** 起算，**tokenId 0 永不出现**。

### 2.6.4 【J-53 裁定】`releaseToDAO` 去权限化（2026-09-14 20:18）

- **任何人可触发**，但**收款地址固定为创建时快照的 `auctionBeneficiary`**，**调用者无法指定收款方**。
- ⇒ 解决了 R9 的**半截**：owner 多签失效**不再导致资金永久卡在拍卖合约**，资金能自动归位。
- **无 griefing 面**：收款方固定 + 条件客观（已铸造）⇒ 触发者只能"提前执行一笔本来就该付的钱"，不能改去向、不能提前（未铸造必 revert）。
- **与出口② 互斥**依旧成立（状态机 `Held→Released` / `Held→Refunded`）。

**⚠️ 两个残余点（必须处理，否则留新坑）**

**（ⅰ）R9 的「铸造」那半截仍未解决。** 【链上查明】`claim` 仅 owner 可调且 JNS 不可升级 ⇒ 多签若在铸造前失效，**域名铸不出来**，`releaseToDAO` 永远过不了校验，只能走 45 天超时退款。这是**预期兜底**（赢家不丢钱），但须在文档中明写：**系统可用性依赖该 2/3 多签存活**。

**（ⅱ）赢家把域名转走后，资金会永久卡死（新发现）** —— 若硬要求 `ownerOf(tokenId) == winner`：
- 赢家领到域名后**转卖/转移** → `ownerOf != winner` ⇒ **`releaseToDAO` 永远 revert**；
- 同时 `_nslookup != 0`（已铸造）⇒ **`claimTimeoutRefund` 也被拒**；
- ⇒ 资金**两条路都走不通，永久锁定**。
- **建议修法**：`releaseToDAO` 判据**只保留** `_nslookup(name) != 0`（域名确已铸造），**删去 `ownerOf==winner` 的硬要求**；把「已铸给赢家」的责任放在铸造流程，并以事件 `MintCompleted(name, tokenId, winner, ts)` 留痕。
  - 若仍想校验收件人，则改为**软校验**：`ownerOf==winner` **或** `tokenId != 0` 两者取其一即放行，避免死锁。
- **（另注）** `claim` 是 `_safeMint(_msgSender())` ⇒ 铸造流程必然先铸给**多签**再转赢家，故「刚 claim 完、尚未转出」的瞬时态下 `ownerOf != winner`，硬校验也会临时卡住。

### 2.6.3 【J-53 裁定·M3】超时窗口可延长、不可暂停

- owner 可**延长** 45 天窗口：每次最多 **+45 天**，**必付 emit 原因**（`TimeoutWindowExtended(old,new,reason,ts)`）。
- **不得**设为无限期 / 不得暂停 `claimTimeoutRefund` ⇒ **保护赢家**（退款权永不被冻结）。

---

### 2.7 状态机

```
[提交] --submitRequest--> Pending
Pending --approveRequest×N--> Approved
Approved --createAuctionFromRequest--> Created(拍卖实例运行: Running)
Running --到期/达标 settle--> Settled
Running --emergencyCancel--> Cancelled
Running --无人出价且到 forceEnd--> Settled(申请人兜底中标)
Settled --45天未铸 claimTimeoutRefund--> Cancelled(退款)
Pending/Approved --reject--> Rejected（可选，见 §11 D-4）
```

---

### 2.8 接口草案（设计层，非实现）

**JNSAuctionFactory**

| 函数 | 权限 | 说明 |
|---|---|---|
| `submitRequest(name, startingPrice, payloadHash)` | 申请人 | 建请求 |
| `approveRequest(requestId)` | 审核人（白名单） | 逐人签名；去重；达 N 转 Approved |
| `createAuctionFromRequest(requestId, durationHours)` | 已授权 | 创建实例 + 同 tx 拉取起拍价 |
| `setReviewer(addr, bool)` | owner | 审核人白名单 |
| `setSignersRequired(uint256)` | owner | 会签门槛（默认 1） |
| `setAuctionBeneficiary(addr)` | owner | 一级收入归属（默认 JNS DAO） |
| `createBatch(...)`（**可选**） | 已授权 | 每场 5 个（WP-5 原文如此），见 §11 D-5 |
| 只读：`getRequest(id)`、`requestsOf(addr)`、`auctionCount()` | 任意 | 观察 |

**EnglishAuction**

| 函数 | 权限 | 说明 |
|---|---|---|
| `bid(amountWJ)` | 任意 | 整数校验 + 最小加价 + 达标才收 WJ |
| `withdrawRefund()` | 本人 | pull 领取（被超越者 / 紧急叫停后） |
| `settle()` | 任意 | 到期触发；幂等 |
| `claimTimeoutRefund()` | 赢家 | 45 天超时（**第 2 批**，需 `ClaimRegistry` 就绪） |
| `emergencyCancel(reason)` | owner | 见 2.5 |
| `setBeneficiary(addr)` | owner | 未结算前 |
| 只读：`minIncrement()`、`nextMinimumBid()`、`isEnded()`、`highestBid`、`highestBidder` | 任意 | 观察 |

---

## 三｜第 2 批｜二级市场设计

| 项 | 设计 |
|---|---|
| 挂拍 | 卖家 `unbind` → `transfer` 到 `SecondaryMarket`（托管） |
| 成交 | 合约**直转赢家**，卖家**无需再参与** |
| 收入 | 扣手续费后**直接转卖家** |
| 手续费 | `tradeFeeRate` 可配置（默认 **1.5%**，区间 1%~2.5%），归 `tradeFeeRecipient`（默认 JNS DAO） |
| JNS 合约 | **不改**（不加 `forceUnbind`，不动核心合约） |
| 起拍价/加价 | **同 §2.3 整数规则** |
| 接口 | `list(tokenId, startingPrice, durationHours)`、`bid(tokenId, amountWJ)`、`withdrawRefund(tokenId)`、`settle(tokenId)`、`cancelListing(tokenId)` |

### ⚠️ 必须澄清：「挂拍即托管…一次性完成」**无法在单笔交易内完成**

【链上查明】JNS 的硬约束：
1. `unbind(tokenId)` **要求 `ownerOf(tokenId) == msg.sender`** ⇒ **第三方（含拍卖合约）无法代调**；
2. `transferFrom` / `safeTransferFrom`（3 个重载）**全部带 `requireUnbound`** ⇒ **未解绑则转不动**。

⇒ 必然是**卖家的两笔交易**：`① unbind` → `② safeTransferFrom(→市场合约)`。
⇒ **中间存在抢转窗口**：卖家在 ① 之后可自行转走。
⇒ **缓解**（沿用 `escrow-jns/Escrow.sol` 先例）：托管合约**校验 `_bound[tokenId] == 0`** 才接收；
窗口风险由**卖家自担 + 仲裁兜底**（卖家抢转 = 违约）。
⇒ 真要原子化必须**改 JNS**（加 `forceUnbind`），**你已明确决定不改** ⇒ 接受上述方案。

---

## 四｜第 3 批｜子域名设计（SubnameRegistry）

| 项 | 设计 |
|---|---|
| 形态 | **必须铸造为 NFT** —— ERC-721（与主域名一致，复用 IERC721 托管/拍卖逻辑） |
| 依据 | 【J-53 裁定】ENS Name Wrapper 支持把子域名 wrap 成 NFT 并可交易；不铸造则持有者无法在链上证明所有权 |
| 部署 | **独立部署**（【链上查明】现有 JNS `_nslookup` 为 `mapping(string=>uint256)` 全串精确匹配，**无层级**） |
| 层级 | 支持**子子域名**（递归层级）；**深度上限 = 5**（`MAX_DEPTH_CEILING = 5`，【J-53 裁定】2026-09-15，与 fangsheng-sudo（JEEP-7）建议 ≤5 对齐；`setMaxDepth` 只能在 1..5 内调整） |
| **label 规则** | **【J-53 裁定·2026-09-15】纯数字**：① 字符集仅 `0-9`（禁字母、禁连字符、禁空串）；② **禁前导零**（`01`/`001` 非法），但 `0` 本身允许——保证「数字 ↔ label」**唯一规范形式**，杜绝 `1`/`01`/`001` 三 NFT 显示相似的钓鱼混淆；③ 长度 ≤ **19**（可安全转 uint64）；④「敏感词风险由父持有者自担」声明**随之移除**（纯数字后风险源头消除，与 JEEP-7「纯数字 = 免审查」对齐） |
| 铸造权限 | **仅主域名持有者**（父为 JNS：`JNS.ownerOf(parentTokenId)==msg.sender`；父为子域名：`ownerOf(parentSubTokenId)==msg.sender`） |
| 再铸开关 | 父可设该子域名**能否再铸子子域名**（`canMintSub`） |
| 父控开关 | `setTransferable(subTokenId, bool)`（可否转移）；`renounceParentControl(subTokenId)`（父**永久**放弃收回权，不可逆） |
| 铸造费 | `mintFee = 1 WJ / 个`，归 `mintFeeRecipient`（默认 JNS DAO） |
| 父域名卖出 | 已铸子域名 NFT **独立存在、不受影响** |
| 扩展位 | 结构体预留**租期字段**位置（v1 不启用） |

**代币模型（设计）**
```
struct SubName {
  address owner;        // 由 ERC-721 承载
  uint256 parentId;     // 父 tokenId
  bool    parentIsJns;  // 父是 JNS 主域名 还是 上层子域名
  string  label;
  uint16  depth;        // 1 = 一级子域名
  bool    transferable;
  bool    canMintSub;
  bool    parentRenounced;
  uint64  mintedAt;
  // ── 扩展位（v1 不用）──
  uint64  leaseStart;
  uint64  leaseEnd;
}
```
**转移拦截**：`_beforeTokenTransfer` 内校验 `transferable`（父未永久放弃前可被父关闭），实现"不可转移"语义。

---

## 五｜第 4 批｜提醒服务设计（NotificationService，仅链上）

### 5.1 边界（**明确不做**）
- ❌ 前端弹窗（DApp 界面，非合约能力）
- ❌ 微信 / 邮件推送（链下服务 + 第三方接口）
- ❌ 链下索引器部署
- ✅ **只提供「订阅 + 扣费 + 余额」能力**，供链下服务将来调用

### 5.2 计费（**整数原则不适用于此**）
- `perUseFee` 可配置（区间 0 ~ 1 WJ；v1 = 0 休眠）；**月费封顶 `MONTHLY_FEE_CAP` = 30 WJ（常量）**
- **按次不得每次提醒都上链扣费**（gas 会吃掉服务费）⇒ **链下记账 + 周期批量结算**
- v1 先实现：**可配置费率 + 批量结算接口** `settleBatch(users[], counts[], periodId)`

### 5.3 资金模式（Q2）

**推荐：模式③ 预授权 + 按需拉取**
用户 `approve` 额度给合约，**钱留在自己钱包**；扣费时 `transferFrom` 拉取，**不得超额**；用户**可随时 revoke**。

**备选：模式① 预付费托管** —— 若实现，必须满足 S1~S4。

| 对比项 | 模式③ 预授权+拉取 | 模式① 预付费托管 |
|---|---|---|
| 首次开户 gas | `approve` ≈ **46k** | `transfer` + 记账 ≈ **70k** |
| 每次扣费（结算时上链） | `transferFrom` ≈ **50k/次**（可批量） | 内部 `SSTORE` ≈ **20k**（更省） |
| 退款/提现 gas | **无需**（钱不在合约） | `transfer` + 校验 ≈ **55k** |
| 合约复杂度 | **低**（无余额账本、无提现权限路径） | **中高**（余额账本 + S1/S2 + S4 上限 + 审计面） |
| 安全面 | **无托管风险** | **有托管风险**，须严格实现 S1~S4 |
| 失败处理 | 用户 revoke ⇒ 结算失败，需 `try/catch` + 欠费标记 | 余额不足 ⇒ 自动停服，无需外部调用 |
| 结论 | ✅ **v1 采用** | 后续如需"预付折扣"再做 |

**⚠️ 模式③ 的关键实现约束**：批量结算中**单用户失败不得回滚整批** ⇒ 必须用
`try/catch`（solc 0.8.0 支持）或 `low-level call` 包装，**逐笔标记欠费**并暂停该用户服务。
（注意：本方案 `SafeERC20` 默认是 revert 语义，批量场景需**另写不 revert 的 `_tryWjTransferFrom`**。）

**★ 欠费上限机制（【J-53 裁定】2026-09-14 18:16，Q2 补充）**

用户 revoke 授权后，链下可能**已提供服务但扣不到费**（`transferFrom` 失败）⇒ 引入**单用户欠费上限**：

| 项 | 设计 |
|---|---|
| 参数 | `maxArrearsPerUser`（**可配置**，默认 **5 WJ**） |
| 记账 | **链下累计**该用户欠费额；链上 `settleBatch` 扣取失败时 emit **`ChargeSkipped`**（含 `batchId / user / amount / reason`），作为**链上锚点** |
| 达到上限 | **链下停止服务**（不再产生新欠费）；链上**主动拦截**：`settleBatch` 对已达上限的用户**跳过**（emit `ArrearsCapped`），不再尝试扣费 |
| 恢复 | 用户重新 `approve`（或充值）后，链下重新提供服务；链上恢复尝试扣费 |
| 与 S2 的关系 | 运营方仍**只能拉取【已消费】部分**；欠费是**运营方自担的信用敞口**，不构成对用户未消费资金的任何权利 |
| 事件 | `ChargeSkipped(batchId, user, amount, reason, ts)`、`ArrearsCapped(user, cumulativeArrears, ts)`、`MonthlyCapReached(user, count, monthlyCharged, ts)` |

**要点**：① 欠费上限**只约束链下服务行为**，链上**不做任何强制划扣**（否则违反 S1/S2）；
② 上限参数由 **owner** 可调；③ 达上限后**不得自动清零**，须留痕（防争议）。

### 5.4 安全要求 S1~S5（**违反即打回**）

| # | 要求 | 落实设计 |
|---|---|---|
| **S1** | 用户可**随时**提取未消费余额，合约**不得限制** | 模式③ 天然满足（钱在用户钱包）；模式① 必须有 `withdrawBalance()` 且无任何锁定期 |
| **S2** | 运营方**只能提取【已消费】部分**，不得挪用未消费资金 | 模式③ 天然满足（拉取额 = 链下记账额）；模式① 须"已消费账本"与"可提现额"严格分离 |
| **S3** | 所有**扣费 / 提现 / 参数变更** emit 事件 | 全事件化（见 §8 T3） |
| **S4** | 单次 `deposit` 设**上限**（防误操作大额转入） | `maxDepositPerTx` 可配置（模式① 必需） |
| **S5** | WJ 转账必经 `_wjSafeTransfer()`（`to≠0` 且 `to≠WJ`） | 全局统一出口（见 §8 T1） |

---

## 六｜收入归属与参数（分模块独立，**禁止全局 feeRecipient**）

| 模块 | 费率参数 | 归属参数 | 默认值 | 可改者 |
|---|---|---|---|---|
| `EnglishAuction` | —（全额） | `auctionBeneficiary` | JNS DAO | **owner** |
| `SecondaryMarket` | `tradeFeeRate`（默认 **1.5%**） | `tradeFeeRecipient` | JNS DAO | **owner** |
| `SubnameRegistry` | `mintFee = 1 WJ/个` | `mintFeeRecipient` | JNS DAO | **owner** |
| `NotificationService` | `perUseFee`（**developer 独占自治**，0~1 WJ；v1=0 休眠） | `serviceFeeRecipient` | JNS DAO（先）→ CGC 决议后切 | **仅 developer**；**owner 只设 `setFeeCap` 上限**（仅可下调）⚠️ |

**设计理由**：未来各模块归属可能**分别调整**，独立参数可**单点变更、互不牵扯**。

**JNS DAO 地址处理（7.2）**
- 合约**不得写死**任何归属地址 ⇒ 全部**可配置参数**，部署后填入
- 每个 setter 必须：**权限保护 + emit 事件**（谁改的 / 从什么改成什么 / 时间）
- 提供**占位实现**（构造参数接受地址，测试期可传部署者 EOA 占位）

---

## 七｜权限矩阵全文（owner / developer / 用户）

### 7.1 三类角色边界

| 角色 | 定义 | 能做什么 | **不能**做什么 |
|---|---|---|---|
| **owner（治理）** | 部署后转入**治理多签**（JNS DAO 治理） | 所有**治理参数与归属**：`auctionBeneficiary` / `tradeFeeRecipient` / `mintFeeRecipient` / **DAO 侧三模块费率** / `setReviewer` / `setSignersRequired` / `emergencyCancel` / `releaseToDAO` / **`setFeeCap`（服务费上限）** | **不得**修改 `serviceFeeRecipient`；**不得**修改 `perUseFee`（developer 独占自治）；**不得**设超 `feeCap` 的费率；**不得**动用用户已锁定/暂存资金 |
| **developer** | 可配置地址（**不写死**） | **自治**：`serviceFeeRecipient` + `perUseFee`（须 ≤ `feeCap` 与常量硬上限 1 WJ）；**变更权仅其本人**（`transferDeveloperRole` 自转让） | **不得**改 JNS DAO 侧任何参数；**不得**超上限设费率；**不得**动用户资金；**owner 亦不得更换 developer** |
| **用户** | 申请人 / 审核人 / 出价者 / 卖家 / 买家 / 子域名铸造者 / 订阅者 | 自己的资金与资产：`submitRequest` / `bid` / `withdrawRefund` / `list` / `mintSubname` / `approve` / `revoke` / `settleBatch` 触发 | 改任何**治理参数**；动**他人**资金 |

> **核心隔离**：`setServiceFeeRecipient` 加 `require(msg.sender == developer)` ⇒ **治理/owner 也不得更改开发者收益归属**。

### 7.2 逐函数权限矩阵

**通用（各合约）**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `transferOwnership(addr)` | ✅ | ❌ | ❌ | `OwnershipTransferred(old,new,ts)` |
| `renounceOwnership()` | 🚫 **已全局禁用**（覆写 revert） | ❌ | ❌ | — |
| `transferDeveloperRole(newDev)` | ❌ | ✅ **仅当前 developer（自转让）** | ❌ | `DeveloperTransferProposed(current, candidate, effectiveAt, expiryAt, ts)` |
| `acceptDeveloperRole()` | ❌ | ✅（**仅被提名人**，须在 [effectiveAt, expiryAt] 窗口内） | ❌ | `DeveloperChanged(old,new,ts)` |
| `cancelDeveloperTransfer()` | ❌ | ✅（**仅当前 developer**） | ❌ | `DeveloperTransferCancelled(current, candidate, ts)` |
| `proposeDeveloper(addr)` | 🚫 **恒 revert**（`DEV: owner cannot change developer`） | 🚫 同 | ❌ | — |
| `vetoDeveloper()` / `acceptDeveloper()` | 🚫 **已停用**（旧入口，恒 revert） | 🚫 同 | ❌ | — |

> **B｜【J-53 裁定·2026-09-15】与语义：沉默 = 否决**
> · **owner/多签无法发起** developer 更换（`proposeDeveloper` 恒 revert）⇒ **多签沉默不可能导致变更**
> · **唯一入口 = 当前 developer 自调 `transferDeveloperRole`**（他人无法代发）
> · **Timelock 语义 = 未确认即不生效**：满 7 天后须由【被提名人主动 `acceptDeveloperRole`】；
>   超过 `expiryAt`（+7 天窗口）未确认 ⇒ **自动作废**（绝非「到期自动生效」）
> · 延迟期内当前 developer 可 `cancelDeveloperTransfer` 撤回
> · **代价（已知且可接受）**：developer 私钥丢失 ⇒ 角色锁死、无法更换；
>   但 v1 提醒服务休眠（`perUseFee = 0`）⇒ **实际影响为 0**（其余四模块不依赖 developer）
> · **回答「J-53 沉默时，变更是否可能生效？」⇒ 否。**

> **`serviceFeeRecipient` 现状确认**：已是 `onlyDeveloper`（owner/多签/他人调用直接 `revert "Not developer"`）
> ⇒ **J-53 沉默即不可改**，无需改造。


**JNSAuctionFactory**

| 函数 | owner | developer | 审核人 | 申请人 | 事件 |
|---|---|---|---|---|---|
| `submitRequest` | — | — | — | ✅ | `RequestSubmitted` |
| `approveRequest` | — | — | ✅（白名单，**硬约束：`require(msg.sender != applicant)` 防自审**） | ❌ | `RequestApproved` |
| `createAuctionFromRequest` | ✅ | ❌ | ✅（也可） | ✅（本人） | `AuctionCreated` |

> **1｜防自审已定为硬约束**：`approveRequest` 内 `require(msg.sender != applicant)`，非建议项。
> **2｜`createAuctionFromRequest` 硬约束**：`require(status[requestId] == Approved)`，未达审核门槛不得建拍卖。

| `setReviewer` | ✅ | ❌ | ❌ | ❌ | `ReviewerSet` |
| `setSignersRequired` | ✅ | ❌ | ❌ | ❌ | `SignersRequiredChanged` |
| `setAuctionBeneficiary` | ✅ | ❌ | ❌ | ❌ | `BeneficiaryChanged` |

**EnglishAuction**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `bid(amountWJ)` | — | — | ✅ 任意 | `Bid` / `Outbid`（被超越退款） |
| `withdrawRefund()` | — | — | ✅ 本人 | `RefundClaimed` |
| `settle()` | — | — | ✅ 任意 | `Settled` / `ApplicantWonInDefault`（兜底中标）/ `MintRequested` |
| `releaseToDAO()` | — | — | ✅ 任意（去权限化） | `WJReleasedToDAO`（**前置 `minted==true` 且 `ownerOf==highestBidder`**） |
| `settleDelivery()` | — | — | ✅ 任意（去权限化） | `DeliverySettled`（**前置 NFT 由多签持有 + 多签已 unbind+授权**） |
| `returnNftToGovernance()` | — | — | ✅ 任意（去权限化） | `NftReturnedToGovernance`（**终态 + NFT 误入本合约**） |
| `claimTimeoutRefund()` | — | — | ✅ **仅赢家本人** | `TimeoutRefunded`（**三分支；`≥ requestedAt + 45d`**） |
| `markMinted(tokenId)` | ✅ | ❌ | ❌ | `MintCompleted`（由 ClaimRegistry 回调或 owner 触发） |
| `emergencyCancel(reason)` | ✅ | ❌ | ❌ | `EmergencyCancelled(reason)` |
| `setBeneficiary(addr)` | ✅ | ❌ | ❌ | `BeneficiaryChanged` |

**SecondaryMarket**

| 函数 | owner | developer | 卖家 | 买家 | 事件 |
|---|---|---|---|---|---|
| `list(tokenId, price, hours)` | — | — | ✅ 本人 | ❌ | `Listed` |
| `bid(tokenId, amountWJ)` | — | — | — | ✅ | `Bid` / `Outbid` |
| `withdrawRefund(tokenId)` | — | — | — | ✅ 本人 | `RefundClaimed` |
| `settle(tokenId)` | — | — | — | ✅ 任意 | `Sold` / `FeePaid` |
| `cancelListing(tokenId)` | ✅（兜底） | ❌ | ✅ 本人（无出价时） | ❌ | `ListingCancelled` |

> **6｜`cancelListing` 兜底须全部挂 pull 退款**：无论卖家本人或 owner 兜底，
> 均须把**所有已出价者**记入 `pendingReturns`，由各人 `withdrawRefund` 自领；
> **禁止**在取消流程内直接批量转账（防 gas 爆炸与重入）。

| `setTradeFeeRate` | ✅ | ❌ | ❌ | ❌ | `TradeFeeRateChanged` |
| `setTradeFeeRecipient` | ✅ | ❌ | ❌ | ❌ | `BeneficiaryChanged` |

**SubnameRegistry**

| 函数 | owner | developer | 父域名持有者 | 子域名持有者 | 事件 |
|---|---|---|---|---|---|
| `mintSubname(parentId,label,to)` | — | — | ✅ | — | `SubnameMinted` |
| `mintSubSubname(...)` | — | — | ✅（上级持有者） | — | `SubnameMinted` |
| `setTransferable(subId,bool)` | — | — | ✅（父，未放弃时） | ❌ | `TransferableSet` |
| `renounceParentControl(subId)` | — | — | ✅（父，**不可逆**） | ❌ | `ParentControlRenounced` |
| `setCanMintSub(subId,bool)` | — | — | ✅（父，未放弃时） | ❌ | `CanMintSubSet` |
| `setMintFee` / `setMintFeeRecipient` | ✅ | ❌ | ❌ | ❌ | `MintFeeChanged` / `MintFeeRecipientChanged` |
| `setMaxDepth` | ✅ | ❌ | ❌ | ❌ | `MaxDepthChanged` |

**NotificationService**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `approve`（WJ，走 ERC20） | — | — | ✅ 本人 | `Approval` |
| `settleBatch(periodStart,periodEnd,users[],counts[],detailHash)` | ❌ | ✅ **专属**（欠费上限内 try/catch 逐笔，不整批回滚） | ❌ | `BatchSettled` / `ChargeSkipped` / `ArrearsCapped` / `MonthlyCapReached` |
| `setPerUseFee(uint256)` | ❌ | ✅ **独占自治**（区间 0 ~ 常量硬上限 `PER_USE_FEE_CAP` = 1 WJ） | ❌ | `PerUseFeeUpdated(old,new,ts)` |
| `setFeeCap(uint256)` | ✅ **多签安全刹车**（仅可下调，下限 0.01 WJ） | ❌ | ❌ | `FeeCapChanged(old,new,ts)` |
| `setServiceFeeRecipient` | ❌ **禁止** | ✅ **专属** | ❌ | `ServiceFeeRecipientChanged` |
| `authorizeForPulling` / `revokeAuthorization` | — | — | ✅ 本人 | `AuthorizedForPulling` / `AuthorizationRevoked` |
| `withdrawBalance()`（仅模式①） | — | — | ✅ 本人（**无条件**，S1） | `BalanceWithdrawn` |

---

## 八｜全局技术要求落实（T1~T8）

| # | 要求 | 落实设计 |
|---|---|---|
| **T1** | WJ 陷阱防护：所有对外 WJ 转账必经 `_wjSafeTransfer()`，首行 `require(to != address(0) && to != WJ_ADDRESS)` + **注释原因** | 各合约**唯一出口** `_wjSafeTransfer()`；**退款、结算、手续费、铸造费、批量扣费**全部经它；`beneficiary`/`recipient` 类 setter **预校验** |
| **T2** | WJ 地址**独立复核**（EIP-55 / 浏览器）；两常量加醒目区分注释 | ✅ **已完成**（见 §0）：`…24203c6AD`（小写 c）PASS；`…2035b89` PASS；**两极相似地址区分注释写入设计** |
| **T3** | **event 写全**（出价/被超越退款/成交/铸造请求/铸造完成/审核通过/紧急取消/超时退款/申请人兜底中标/费率变更/归属变更/订阅扣费），每个含**域名+相关地址+金额+时间戳** | 全事件表（见 §9.3），均带 `nameOrTokenId / addresses / amountWJ / block.timestamp` |
| **T4** | 合约**不可升级**（与 JNS/WJ 惯例一致） | 无 proxy / UUPS / `initialize`；修复靠**新模块持续交付** |
| **T5** | 引入 `IERC20` + `SafeERC20` | 统一依赖层；批量场景另备**不 revert** 变体 |
| **T6** | v1 **不做合约内 wrap**；文档引导用户**先 wrap 再出价** | 全合约**无** `deposit`/`wrap` 调用；README 引导：「先把 J wrap 成 WJ，再 approve+出价」 |
| **T7** | 避免"同一 tx 内出价即成交" | **条件互补反证**：`bid` 要求 `ts < endTime`；`settle` 要求 `ts >= endTime` ⇒ **不存在同时成立的时刻** ⇒ 即便走 flashLoan 也无法同 tx 成交 |
| **T8** | 编译 `solc --evm-version istanbul`，**0 error / 0 warning** | 编译门禁；**交付前必跑**（上一批已实证可达成） |

---

## 九｜风险点清单

### 9.1 高风险（🔴）

| # | 风险 | 影响 | 处置 |
|---|---|---|---|
| **R-1** | **WJ 陷阱**：转给 `0x0` 或 WJ 合约 ⇒ 烧 WJ + 退 J 给 `msg.sender`（本合约）⇒ 合约无提款入口，**永久损失** | 资金损失 | **T1** 唯一出口防护 + 全部 setter 预校验 |
| **R-2** | **45 天超时退款与"款项进 DAO"冲突**（§2.6）：钱若已付 DAO，则退款无来源 | 赢家拿不到退款 | **须裁定**（§11 D-1）；推荐方案 A（铸造前托管） |
| **R-3** | **二级市场"一次性托管"不可原子**（§三）：JNS `unbind` 硬要求 `ownerOf==msg.sender`，`transferFrom` 带 `requireUnbound` ⇒ **必然两笔交易 + 抢转窗口** | NFT 被抢转 | 托管合约校验 `_bound==0`；卖家自担 + 仲裁；**不改 JNS**（已裁定） |
| **R-4** | **emergencyCancel 权限集中在 owner** ⇒ 单一主体可叫停拍卖 | 治理风险 | 建议 owner = **Timelock/多签**，非 EOA |

### 9.2 中风险（🟡）

| # | 风险 | 处置 |
|---|---|---|
| **R-5** | **自审绕过**：申请人 == 审核人 | 建议加 `require(reviewer != applicant)`（除非明确允许）；见 §11 D-3 |
| **R-6** | **申请人"必付"义务**（§2.2）：无人加价则申请人须买入 | **设计意图**，但须在产品文案显著提示 |
| **R-7** | **整数规则下限价区最小加价占比极高**（1 WJ → 加价 1 WJ = 100%） | 设计接受（你已明确 ceil 规则） |
| **R-8** | **提醒服务模式③ 用户 revoke ⇒ 批量结算失败** | `try/catch` + 欠费标记 + 暂停服务；**不整批回滚**；**已加单用户欠费上限（默认 5 WJ，可配置），达上限链下停服**（【J-53 裁定】2026-09-14） |
| **R-9** | **链下记账 vs 链上不一致** | 批结算 `periodId` + 明细 hash 上链留痕 + `BatchSettled` 事件 |
| **R-10** | **费率/归属变更的生效时点未定**：进行中的拍卖是否受影响 | 已裁定：**仅「有生命周期」的实例才快照**（拍卖：创建→竞价→settle→放款）；**即时型（子域名 mintFee、二级市场单笔成交费、提醒服务扣费）每次读当前值，不快照**；见 §11 D-2 |
| **R-11** | **手续费取整方向未定** | 建议 `tradeFee = amount × rate / 10000`（**floor**，对卖家有利）+ **整数化**；须明确 |
| **R-12** | **子域名父控语义未定**：父是否有"收回权"（强制夺回/销毁子 NFT） | 见 §11 D-6；v1 建议**无收回权**，父控仅限切换标志位 |
| **R-13** | **递归层级 gas / 栈深** | `maxDepth` 参数封顶。**【J-53 裁定·2026-09-15】`MAX_DEPTH_CEILING` 8 → 5**（与 fangsheng-sudo JEEP-7 建议 ≤5 对齐）。影响评估：改 1 处常量，**零影响**（无用例/存储依赖 depth>2）；改后 124/124 全过 |

### 9.3 低风险（🟢）与事件表（T3）

| # | 风险 | 处置 |
|---|---|---|
| **R-14** | 重入（`bid`/`withdrawRefund`/`settle`/`settleBatch`） | `nonReentrant` + **CEI**（先改状态后转账） |
| **R-15** | ERC-677 回调（WJ 的 `*AndCall`） | 本设计**只用 `transfer`/`transferFrom`**，不触发回调 |
| **R-16** | 不可升级 ⇒ 修复需**新合约** | 子域名 NFT 依赖新合约持续可用（**无迁移路径**），须公告 |
| **R-17** | `data:` / `string reason` 过长耗 gas | `emergencyCancel` 限长或改传 `bytes32`（见 §11 D-7） |

**事件表（T3，全部含 域名/相关地址/金额/时间戳）**

| 事件 | 字段 |
|---|---|
| `RequestSubmitted` | requestId, applicant, name, startingPrice, payloadHash, ts |
| `RequestApproved` | requestId, reviewer, approvals, ts |
| `RequestRejected`（可选） | requestId, reviewer, ts |
| `AuctionCreated` | requestId, auction, name, startingPrice, ts |
| `Bid` | auction, name, bidder, amount, newEndTime, ts |
| `Outbid` | auction, name, outbidder, refundAmount, ts |
| `Extended` | auction, newEndTime, ts |
| `RefundClaimed` | auction, user, amount, ts |
| `Settled` | auction, name, winner, amount, beneficiary, ts |
| `ApplicantWonInDefault` | auction, name, applicant, amount, ts |
| `MintRequested` | auction, name, winner, reviewRef, ts |
| `MintCompleted` | auction, name, winner, tokenId, ts |
| `EmergencyCancelled` | auction, name, reason, ts |
| `TimeoutRefunded` | auction, name, winner, amount, ts |
| `TradeFeeRateChanged` | market, oldBps, newBps, ts |
| `MintFeeChanged` | oldFee, newFee, ts |
| `BeneficiaryChanged` | oldAddr, newAddr, ts |
| `DeveloperTransferProposed` / `DeveloperTransferCancelled` / `DeveloperChanged` | current/candidate/old/new, ts |
| `SubnameMinted` | parentId, subId, label, owner, depth, fee, ts |
| `TransferableSet` | subId, transferable, ts |
| `ParentControlRenounced` | subId, ts |
| `Subscribed` / `Unsubscribed` | user, plan, ts |
| `BatchSettled` | batchId, user, count, amountWJ, periodStart, periodEnd, ts |
| `ChargeSkipped` | batchId, user, amount, reason, ts |
| `ArrearsCapped` | user, cumulativeArrears, ts |
| `MonthlyCapReached` | user, count, monthlyCharged, ts |
| `PerUseFeeUpdated` / `FeeCapChanged` | old, new, ts |
| `BalanceWithdrawn`（仅模式①） | user, amount, ts |
| `Sold` | market, tokenId, seller, winner, price, fee, ts |

---

## 十｜Q1~Q5 建议

### Q1｜审核单人还是会签？
**建议：默认单人（`signersRequired = 1`），合约支持会签。**

理由：① 当前 JNS DAO 审核是**人工作业、量小**，会签会显著拉长「申请→上架」时延；
② 单人社群决策链短，**责任仍具体到人**（审核人地址上链）；
③ 合约层已把门槛做成参数，未来转会签**零改码**（只需 `setSignersRequired(2)` + 增加审核人白名单）。
**建议同时**：加 `require(reviewer != applicant)` 防自审（见 §11 D-3）。

### Q2｜提醒服务资金模式？
**建议：模式③ 预授权 + 按需拉取。**

理由：① **无托管风险**，天然满足 S1/S2；② 合约面**小得多**（无余额账本、无提现权限），**审计成本低**；
③ gas 上，模式③ 把成本压在**一次性 approve(46k)** 与**结算期 transferFrom(50k/次)**；
模式① 单次扣费更省（`SSTORE` 20k）但需额外承担 **提现 transfer(55k)** 与更复杂的安全实现。
④ 唯一代价：用户 revoke 会导致结算失败 ⇒ 用 `try/catch` + 欠费标记处理（**不整批回滚**）。
⇒ **v1 用模式③**；将来若要做"预付享折扣"，再加模式① 作为可选档位。

### Q3｜提醒计费：按次 0.05 还是包月？
**建议：启用时按次 `perUseFee = 0.05 WJ`（可调区间 0 ~ 1 WJ）；月费为封顶 30 WJ（`MONTHLY_FEE_CAP` 常量）——付满后当月继续提醒、不再扣费（A 包月制）。**

理由：① 按次与**实际用量**直接挂钩，对低频用户更公平；
② 与模式③（预授权拉取）**天然匹配**——不需要处理"未用完的月费如何退"；
③ 包月需引入**按比例退款/结转**逻辑，复杂度与争议都更高，宜等有真实高频用户后再启用。
⇒ 两参数都**预留可配置**，默认走按次。

### Q4｜超时退款 45 天是否合适？
**建议：合适，保留 45 天为默认值 + 可配置开关（`timeoutRefundEnabled` / `stalePeriod`）。**

理由：① 【链上查明】JNS `claim` 仅 owner（2/3 多签）可调，**每场需 2 步多签操作**，
人工流程有排队与假期因素，45 天（6.4 周）足够宽裕；
② 过短会**误伤正常流程**（把正常多签延迟判成违约），过长则赢家资金长期悬空；
③ 做成 owner 可调 + 可关闭，兼顾未来变化。
（**前置依赖**：须先裁定 §11 D-1 的资金托管路径，否则退款无来源。）

### Q5｜二级市场手续费率默认？
**建议：默认 `tradeFeeRate = 1.5%`（落在你给的 1%~2.5% 中位）。**

理由：① 相较主流 NFT 市场（OpenSea 2.5%、Blur 0.5%），1.5% 属**中低档**，鼓励二级流通；
② v1 无仲裁成本转嫁需求（仲裁靠人工），不需要高费率覆盖；
③ 参数可配置，后续按实际运营数据调整。

---

## 十一｜设计待确认点（超出 Q1~Q5；按「含糊即停机」原则列出，**不自行决定**）

| # | 待确认点 | 说明 | 我的建议 |
|---|---|---|---|
| **D-1** | **结算资金路径**（§2.6）：铸造前托管（方案 A）还是 settle 即付 DAO（方案 B）？ | 与 3.2「款项进 JNS DAO」+ 3.6「45 天全额退」存在冲突 | **方案 A**（托管至 mint 完成），否则超时退款无来源 |
| **D-2** | **费率/归属变更的生效时点** | 影响已出价者预期 | ✅ **已裁定（2026-09-14 20:18）**：**按「有无生命周期」分流**，见下方 D-2 详述 |

### D-2 详述　费率/归属变更的生效时点（【J-53 裁定】2026-09-14）

**总原则：快照只适用于【有生命周期】的对象。**

| 类型 | 模块 | 生效时点 | 理由 |
|---|---|---|---|
| **有生命周期**（创建→竞价→settle→放款） | `EnglishAuction`（含受益人快照） | ✅ **创建时快照费率 + 受益地址** | 开拍后即锁定预期，防中途改规则损害已出价者 |
| **即时型・每次读当前值** | **`SubnameRegistry.mintFee`** | ❌ **不快照** | 铸造是**即时**动作；快照会导致**改 mintFee 永远不生效** |
| **即时型・每次读当前值** | `SecondaryMarket.tradeFeeRate` | ❌ **不快照**（**待确认**，见下） | 单笔成交为一瞬间，按成交时当前费率 |
| **即时型・每次读当前值** | `NotificationService.perUseFee` | ❌ **不快照** | 批量结算时按**结算时点**当前费率 |

**⚠️ 衍生待确认（需你定）**：二级市场 `SecondaryMarket` 存在「挂单→竞价→成交」的生命周期，但**成交费只在 `settle` 一瞬间扣除**。两种取法：
- **(甲) 成交时取当前费率**（推荐，符合「即时」语义，实现简单）；
- **(乙) 挂单时快照费率**（保护挂单者预期，但与 `list` 后 owner 调费率会「晚改不生效」）。

**同理适用**：`auctionBeneficiary` 等**归属**参数亦沿此分流——拍卖创建时快照，子域名/提醒服务**每次读当前值**。
| **D-3** | **是否允许自审**（申请人 == 审核人）？ | 单人模式下易发生 | **禁止**：`require(reviewer != applicant)` |
| **D-4** | 是否需要**显式拒绝**（`rejectRequest`）？ | 指令未提；无拒绝则请求永久挂 Pending | 建议加，便于治理留痕 |
| **D-5** | 是否保留 **`createBatch`（每场 5 个）**？ | 新指令未提；WP-5 specs 原文为「每场拍卖 5 个 JNS 域名」 | **保留为可选**，不强制 |
| **D-6** | **父控的"收回权"**到底指什么？父能否强制夺回/销毁已铸子 NFT？ | 「renounceParentControl（父永久放弃收回权）」暗示默认有收回权 | v1 **不设收回权**；父控仅限切换子域名标志位 |
| **D-7** | `emergencyCancel(reason)` 的 reason 用 `string` 还是 `bytes32`？ | `string` 直观但 gas 高、无长度上限 | 用 `string` + **长度上限 200**（留可读性） |
| **D-8** | 二级市场**手续费取整方向**？ | 影响卖家/DAO 谁得碎屑 | `floor`（对卖家有利）+ 金额整数化 |
| **D-9** | 提醒服务**结算周期**（每日/每周）？ | 指令写"每日/每周"未定 | **每周**（gas 更低，争议窗口可接受） |
| **D-10** | 各合约 **owner 最终是谁**？（EOA / 多签 / Timelock） | 影响 emergencyCancel 风险 | **Timelock 或多签**，非 EOA |

---

## 附录 A｜设计依据

> 本设计依据 2026-09-14 的项目整合指令（取代此前全部零散指令）编制；
> 指令要点已逐项落实于本文件各章节。

---

## 附录 B｜分批交付记录与后续裁定（2026-09-14）

### B-1　第一批（已交付 · 已审核通过）
`EnglishAuction.sol`（282 行）+ `JNSAuctionFactory.sol`（222 行）+ `deps/Deps.sol`（184 行）。
编译：solc 0.8.0 / `--evm-version istanbul` / optimizer 200 ⇒ **0 error / 0 warning**。

**审核后加固（【J-53 裁定】）**：`releaseToDAO` 由「仅告警的软校验」改为 **白名单软校验**：

```solidity
address curOwner = IJNS(JNS_ADDRESS).ownerOf(tokenId);
require(curOwner == highestBidder || curOwner == IJNS(JNS_ADDRESS).owner(),
        "EA: minted to unexpected address");
```

| 情形 | 结果 |
|---|---|
| 铸给 winner | ✅ 通过 |
| 铸给多签（`claim` 后未转出的中间态） | ✅ 通过 |
| 铸给无关第三方 | ❌ revert（资金停 `Held`，等治理纠正） |

保留 `WinnerOwnershipMismatch` 事件供链下监控。目的：避免「铸错人还照付钱、赢家两头空」。

**新增风险条目（【J-53 裁定】）**：**铸错人情形的治理救济路径** —— revert 后资金停 `Held` → 治理纠正 → 重触发 `releaseToDAO`；长期无法纠正走 `extendTimeoutWindow` + 治理特别决议。赢家不会两头空，但**需治理介入**。

### B-2　两项裁定（2026-09-14 20:35）

| 编号 | 事项 | 裁定 |
|---|---|---|
| **B-2-②** | Timelock | **v1 不接**。受益地址变更频率极低 + 已有 2/3 多签把关 ⇒ 保持 `onlyOwner + emit`，**接口位保留**（后续可平滑接入独立 Timelock 合约） |
| **B-2-③** | 二级市场费率取值 | 采纳 **(甲) 成交时取当前费率**；费率变更**必须 emit** + 建议提前公告 |

(甲) 的实现即 `settle()` 内直接读 `tradeFeeRate`（不写入 `Listing` 快照）。

### B-3　第二批（已交付）
`SecondaryMarket.sol`（231 行）+ `ClaimRegistry.sol`（140 行）。
编译：同上 ⇒ **0 error / 0 warning**。

**`ClaimRegistry` 职责收缩（【J-53 裁定】）**：M1 改链上校验后，本合约**不再是放款前置条件**，职责调整为**【待铸造任务登记】**——记录 `auctionId / name / winner / amount` 供多签读取执行 `JNS.claim()`。**不持有资金、不阻塞放款**。

**二级市场要点**：挂拍即托管（卖家先自行 `JNS.unbind`，本合约同 tx 拉取 NFT）｜成交后合约**直接转 NFT 给赢家**，卖家无需参与｜`tradeFeeRate` 1.5%（成交时取当前值）｜手续费归 `tradeFeeRecipient`、余款转卖家｜起拍价与加价同整数规则｜**不改 JNS 合约**｜所有 WJ 转账必经 `_wjSafeTransfer`。

---
