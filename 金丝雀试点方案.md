# 金丝雀试点方案（Canary Pilot）

> 生成：2026-09-14 · **只读核查，未执行**
> 背景：Jouleverse【**无测试网**】+ 四批合约【**不可升级**】⇒ 无法先在小规模链上验证后修正。
> 因此主网首秀必须用**最小成本 + 废弃域名 + 仅内部三人**跑通全链路，通过后才开放公开拍卖。

---

## 一、试点边界（硬约束）

| 项 | 取值 | 理由 |
|---|---|---|
| 标的名 | **`test-pilot-1.j`**（废弃/测试域名） | 万一失败也不影响真实命名空间 |
| 起拍价 | **1 WJ**（合约允许的最小整数价） | 成本最小；且 1 WJ 可验证 ceil 下限路径 |
| 参与人 | **仅三位多签持有人** | 无外部资金风险；问题可控 |
| 时长 | 168h（默认）或按需缩短 | 缩短可加快验证 |
| 等级 | 内部试点，**不公开** | 通过后才放开 |

---

## 二、试点步骤（按序）

### 步骤 0｜部署与初始化（一次性）
1. EOA 部署者执行 `Deploy.s.sol`（owner = 多签）
2. 多签 2/3 执行 §3b 两条：`setReviewer(J0)`、`setRegistrar(factory, true)`
3. 校验：`factory.isReviewer(J0) == true`、`registry.isRegistrar(factory) == true`

### 步骤 1｜申请（申请人 = 试点人 A）
- `factory.submitRequest("test-pilot-1.j", 1e18, payloadHash)`
- **断言**：`RequestSubmitted` 事件；`getRequest(0).status == Pending`

### 步骤 2｜审核（审核人 = J-0 EOA，**非申请人**）
- `factory.approveRequest(0)`（由 J-0 本人钱包发起）
- **断言**：`RequestApproved(0, J0, 1, ts)`；`status == Approved`
- **反证**：若 J-0 恰为申请人，则 `require(msg.sender != applicant)` 必 revert（防自审）

### 步骤 3｜开拍（同 tx 拉起拍价）
- A 先 `WJ.approve(factory, 1e18)`，再 `factory.createAuctionFromRequest(0, 168)`
- **断言**：`AuctionCreated`；拍卖合约 WJ 余额 == 1e18；`highestBidder == A`、`highestBid == 1e18`
- 多签补 `registry.setRegistrar(auction, true)`

### 步骤 4｜出价（B 出 2 WJ，恰好 = 1 + ceil(5%)）
- B 先 `approve(auction, 2e18)`，再 `auction.bid(2e18)`
- **断言**：`Bid` + `Outbid`；A 的 `pendingReturns == 1e18`；合约余额 == 3e18
- **边界反证**：1 WJ → 1 WJ 必须 revert（`below minimum`）

### 步骤 5｜settle（到点后任何人可调）
- 等待 `block.timestamp >= endTime`，调 `auction.settle()`
- **断言**：`Settled` + `MintRequested`；`escrow == Held`；合约仍持 3 WJ（**未放款**）

### 步骤 6｜铸造（多签调 `JNS.claim`）
- 多签执行 `JNS.claim("test-pilot-1.j")` ⇒ `_safeMint(多签)`（**先铸给多签**）
- 多签再把 NFT 转给赢家 B（`transferFrom`，需先 `unbind`）
- **断言**：`JNS._nslookup("test-pilot-1.j") != 0`；`ownerOf(tokenId) == B`（或在 B 已转走时为 B 的受让人）

### 步骤 7｜放款（**任何人**可调）
- 调 `auction.releaseToDAO()`
- **断言**：`ownerOf ∈ {winner, JNS.owner()}` 通过白名单校验；`WJReleasedToDAO`；`escrow` 由 `Held → Released`

---

## 三、预期断言汇总（验收清单）

| # | 断言 | 判据 |
|---|---|---|
| 1 | `escrow` 终态 = **Released**（枚举值 2） | `auction.escrow()` |
| 2 | 款项到 **`0x4eF599…C1B3`**（多签） | `WJ.balanceOf(多签)` 增加 `highestBid` |
| 3 | 事件齐全 | `RequestSubmitted`/`RequestApproved`/`AuctionCreated`/`Bid`/`Outbid`/`Settled`/`MintRequested`/`WJReleasedToDAO` |
| 4 | 前手退款可领回 | A 调 `withdrawRefund()` 后余额 +1 WJ |
| 5 | **WJ 零陷阱** | `WJ.transfer` 无 `to=0/WJ` 分支触发；合约无滞留在途 |
| 6 | 对账无尘差 | `WJ.balanceOf(auction) == 0`（放款 + 退款完成后） |
| 7 | 两出口互斥 | release 后再 `claimTimeoutRefund` 必 revert |
| 8 | **主网行为与本地一致** | 各步 revert 原因/事件与 `forge test` 结果逐条吻合 |
| 9 | `_nslookup` 判据成立 | 铸造前 `== 0`（release 必 revert）、铸造后 `!= 0`（release 通过） |

---

## 四、成本估算

> 实测（2026-09-14，主网）：`eth_gasPrice` = `0x9502f907` = **2.5 gwei**；多签余额 = `20001000000000000 wei` ≈ **0.0200 J**（**余额不足**，须先补）。

| 项 | gas | 折算 J | 说明 |
|---|---|---|---|
| 部署五合约 | **12,848,277**（干跑实测） | **0.03212 J** | Part 1 `Deploy.s.sol` |
| 初始化 2 条多签 setter | ≈ 0.20M | ≈ 0.00050 J | §3b |
| developer 就位（propose+accept） | ≈ 0.10M | ≈ 0.00025 J | §3c |
| 全流程 7 步（申请→审核→开拍→出价→settle→claim→release） | ≈ 1.50M | ≈ 0.00375 J | 含 pull 领回 |
| **合计（不含标的款）** | **≈ 14.65M** | **≈ 0.0366 J** | — |
| 标的款（WJ） | — | **1 WJ**（起拍价）；若 B 加价则 2 WJ | 全数回流多签/申请人 |

> ⚠️ **余额预警**：多签当前 **0.0200 J** < 需求 **≈0.0366 J**。试点前**必须先给部署 EOA / 多签补足原生 J**（建议 ≥0.05 J）。

> ⚠️ **余额预警**：多签当前仅 ≈0.0207 J，**不足以**支付部署 + 全流程气费（需 ≈0.037 J）。试点**前必须先给部署 EOA / 多签补足原生 J**。

---

## 五、通过标准与后续

**全部满足方可通过**：§三 的 9 条断言 ✓、§四 成本在预算内 ✓、主网行为与本地测试逐条一致 ✓。

**通过后**：才开放公开拍卖。
**未通过**：停机报告，**不得**以「再试一次」方式绕过 —— 合约不可升级，必须先在本地复现并修好测试/设计，再决定是否重跑试点。

---

## 六、风险提示

| 风险 | 说明 | 缓解 |
|---|---|---|
| 不可升级 | 部署即定终局 | 试点前置 + 参数逐项核对（见《部署与多签执行清单》§5） |
| 无测试网 | 无法预演 | 本地 forge 全量测试 **140/140** 通过（历史基线 117）+ 试点最小化 |
| 多签气费不足 | 当前 ≈0.0207 J < 需求 ≈0.037 J | 试点前补足原生 J |
| 铸错人 | `claim` 先铸多签再转出 | 白名单软校验（winner 或多签）；异常则 revert 等治理纠正 |
| 多签失效（未铸前） | `claim` 仅 owner 可调 ⇒ 铸不出 | 45 天超时退款兜底；需治理介入 |
