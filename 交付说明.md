# JNS 拍卖系统 · 第一批交付（EnglishAuction + JNSAuctionFactory）

> 2026-09-14｜**只读环境交付**：未部署、未上链、未 commit、未 push
> 编译验证：`solc 0.8.0 --evm-version istanbul --optimizer 200` → **0 error / 0 warning**
> 文件：`jns-auction/contracts/EnglishAuction.sol`(236行)、`JNSAuctionFactory.sol`(186行)、`deps/Deps.sol`(105行)

---

## 零｜编译与字节码

| 合约 | 源码 | bytecode | deployedBytecode |
|---|---|---|---|
| `EnglishAuction` | 236 行 | 6,781 B | 5,428 B |
| `JNSAuctionFactory` | 186 行 | 12,389 B | 11,831 B |

命令（可复现）：
```bash
npx --yes solc@0.8.0 --standard-json < in.json   # evmVersion: istanbul, optimizer runs 200
```

**关于 `deps/Deps.sol`**：内含 `IERC20` / `SafeERC20` / `Ownable` / `ReentrancyGuard` 四个**接口一致、行为等价**的最小实现。
-**原因**：本环境无 `node_modules`、无离线 OZ 包，自带依赖才能**离线编译通过**（这是本次唯一的"额外"文件，非合约逻辑）。
- **替换方式**：若目标仓库已装 OZ 4.x，把该文件删掉、四个 import 换成 OZ 路径即可，**两个合约源码无需任何改动**（所用 API 完全一致）。

---

## 一｜WJ 硬约束逐条落实（WJ1~WJ6）

| 约束 | 要求 | 落实位置（源码行） | 状态 |
|---|---|---|---|
| **WJ1** | 支付一律 WJ；出价 `WJ.transferFrom(bidder,this,amount)`；退款退 WJ | `bid()` → `SafeERC20.safeTransferFrom(IERC20(WJ_ADDRESS), msg.sender, address(this), amount)`；退款 `withdrawRefund()` → `_wjSafeTransfer`；结算 `settle()` → `_wjSafeTransfer`。**全合约无 `payable`、无 `msg.value`** | ✅ |
| **WJ2** | 任何对外 WJ 转账须 `require(to != 0 && to != WJ合约)` | **`_wjSafeTransfer()`（唯一出口）** 首行 `require(to != address(0) && to != WJ_ADDRESS, "WJ trap: ...")`，并**附注释说明原因**。退款/结算**两处均必经此函数**；构造函数与 `setBeneficiary` 也预校验受益人 | ✅ |
| **WJ3** | reservePrice 单位 WJ；`minIncrement = max(最高价×5%, 0.01 WJ)` | `minIncrement()`：`pct = highestBid * 5 / 100; return pct > MIN_INCREMENT_FLOOR ? pct : MIN_INCREMENT_FLOOR;`，`MIN_INCREMENT_FLOOR = 0.01 ether`（=1e16，WJ decimals=18） | ✅ |
| **WJ4** | 引入 IERC20 + SafeERC20 | `import "./deps/Deps.sol"`；用量 `safeTransfer` / `safeTransferFrom` | ✅ |
| **WJ5** | v1 不做合约内 wrap | 全合约**无** `deposit`/`depositTo`/`withdraw`(WJ的) 调用；只做 WJ 转账，不碰 J | ✅ |
| **WJ6** | 避免"同一 tx 内出价即成交" | `bid()` 要求 `block.timestamp < endTime`；`settle()` 要求 `block.timestamp >= endTime` ⇒ **条件互补，物理上不可能同 tx**（见 `settle()` 顶部注释） | ✅ |

**其余既定参数**：168h 默认时长 ✅｜末 10 分钟出价 → +10min ✅｜总时长上限 192h ✅｜须达保留价 ✅｜pull 退款 ✅｜`block.timestamp` ✅｜工厂 5 实例（`createBatch` 强制 `BATCH_SIZE=5`）✅｜`--evm-version istanbul` ✅

---

## 二｜权限矩阵

### EnglishAuction

| 函数 | 调用者 | 权限 | 说明 |
|---|---|---|---|
| `bid(amount)` | 任何人 | 无限制 | 需先 `WJ.approve(this, amount)`；未开始/已结束/已结算/已撤销 均 revert |
| `withdrawRefund()` | 被超价者本人 | 无限制 | pull 模式，仅能领自己的 `pendingReturns` |
| `settle()` | 任何人 | 无限制 | 到期后可触发；幂等（`settled` 标志） |
| `minIncrement()` / `nextMinimumBid()` / `isEnded()` | 任何人 | view | 只读 |
| `cancel()` | **owner（工厂）** | `onlyOwner` | **仅限无人出价时**；有出价则 revert |
| `setBeneficiary(addr)` | **owner（工厂）** | `onlyOwner` | 未结算前可改；禁止 0/WJ 地址 |
| `transferOwnership(addr)` | owner | `onlyOwner` | 继承自 Ownable |

### JNSAuctionFactory

| 函数 | 调用者 | 权限 | 说明 |
|---|---|---|---|
| `createAuction(...)` | **owner 或被授权人** | `onlyApproved` | 人工审核后创建；受益人 = `defaultBeneficiary` |
| `createAuctionWithBeneficiary(...)` | **owner 或被授权人** | `onlyApproved` | 本次指定受益人 |
| `createBatch(names[5],prices[5],...)` | **owner 或被授权人** | `onlyApproved` | 强制 5 个（对齐 WP-5） |
| `setApprovedRequester(addr,bool)` | owner | `onlyOwner` | 授权运营岗 |
| `setDefaultBeneficiary(addr)` | owner | `onlyOwner` | 一级收入去向，待 J-53 裁定后设定 |
| `cancelAuction(addr)` | owner | `onlyOwner` | 工厂转发（工厂是拍卖实例的 owner） |
| `setAuctionBeneficiary(addr,addr)` | owner | `onlyOwner` | 转发 |
| `auctionCount()` / `allAuctions()` / `batchCount` | 任何人 | view | 只读 |

> **所有权链路**：工厂 owner = 治理多签（构造时 `_transferOwnership(owner_)`）；拍卖实例 owner = **工厂合约**。
> **新增**：`EnglishAuctionDeployer` 的 `owner` = 部署广播者（构造时锁定），仅一次性 `setFactory(factory)` 绑定工厂；`deploy()` 仅工厂可调。该 owner **不接触资金/NFT、不能改已绑 factory、不能直接 deploy**，与「无人主持」目标相容（绑定为部署期一次性动作）。
> ⇒ 日常治理动作（撤销、改受益人）走「多签 → 工厂 → 实例」，链上留痕清晰。

---

## 三｜风险点（含已知取舍）

| # | 风险 | 等级 | 现状 / 处置 |
|---|---|---|---|
| R-1 | **WJ 陷阱**：转给 `0x0` 或 WJ 合约 → 烧 WJ + 退 J 给本合约，**合约无提款入口 ⇒ 永久损失** | 🔴 高 | **已双重防护**：① `_wjSafeTransfer` 统一 require；② 构造/setter 预校验 beneficiary。**退款、结算两处出口都走同一函数** |
| R-2 | **`beneficiary` 若设为多签以外的合约且该合约不处理 WJ** → 资金卡死 | 🟡 中 | 待 J-53 裁定收入去向；建议设为**多签 EOA/多签合约**（`0x3B717119878E2db1AA7df46F5AdcF9766A01706F`） |
| R-3 | **`settle()` 依赖外部调用**：若无人调用，资金与标的挂起 | 🟡 中 | 设计为**任何人可调**（含中奖者/运营）；可另配链下 bot 兜底 |
| R-4 | **重入** | 🟢 低 | `nonReentrant` 覆盖 `bid`/`withdrawRefund`/`settle`；且已遵循 checks-effects-interactions（先改状态后转账） |
| R-5 | **ERC-677 回调风险**：WJ 是 `transferAndCall` 合约吗？本合约只用 `transfer`/`transferFrom`，**不触发回调** | 🟢 低 | 未使用 `*AndCall` 系列，故不受 hook 影响 |
| R-6 | **域名铸造尚未接通**：`MintRequested` 事件已发，但 `JNSMinterController` 属**下一批** | 🟡 中 | 本批仅到「拍卖结算 + 事件广播」；**中标者拿到的是债权凭证，域名需下一批交付后铸造** |
| R-7 | **出价与结算的时效**：极端情况下最后 10 分钟内连续出价，最多延至 `startTime+192h` | 🟢 低 | 已按既定规则封顶 |
| R-8 | **首笔出价 `reservePrice=0` 时无下限**（仅要求 `>0`） | 🟡 中 | 若不允许 0 WJ 起拍，须由工厂/审核侧拒绝 `reservePrice=0`；**待 J-53 裁定** |
| R-9 | **`cancel()` 有出价时 revert**，但此时若 `settle` 也未被调用，需等到期 | 🟢 低 | 符合既定"有出价不可撤销" |
| R-10 | **未做链上 reserve 的"审核留痕"校验**：`reviewRef` 仅作锚点，合约不验证其内容 | 🟢 低 | 按设计：审核在链下，链上只留哈希 |

---

## 四｜自测用例（含 WJ 陷阱防护两项）

> 说明：本环境**无 EVM 执行器**（无 hardhat/foundry、无离线 npm 包），下述用例为**按源码逐行推导**的设计稿，
> **尚未在 EVM 上执行**。标记为「待执行」；给出**预期结果**与**断言**，便于在目标仓库（truffle/hardhat）直接落地。
> 已执行部分：**静态编译 0 error / 0 warning**（见第零节）。

### A 组｜基本拍卖流程

| # | 用例 | 步骤 | 预期 | 状态 |
|---|---|---|---|---|
| A-1 | 首笔出价须 ≥ 保留价 | `reservePrice=100 WJ`；bid(90) | revert `EA: below minimum` | 待执行 |
| A-2 | 首笔达标出价成功 | bid(100)，已 approve | `highestBid=100`、`highestBidder=caller`、WJ 到合约 | 待执行 |
| A-3 | 后续出价须 ≥ 当前+5% | 当前 100 → 最小 105；bid(104) | revert `EA: below minimum`（104 < 105） | 待执行 |
| A-4 | 5% 与 0.01 取大 | 当前 0.1 WJ → 5%=0.005 < 0.01 ⇒ 最小 0.11 | bid(0.105) revert；bid(0.11) 通过 | 待执行 |
| A-5 | 被超价者进入 pull 台账 | A bid(100) → B bid(105) | `pendingReturns[A]=100`；A 调 `withdrawRefund()` 得 100 WJ | 待执行 |
| A-6 | 防狙击延期 | 距结束 9 分钟内 bid | `endTime += 10min`，emit `Extended` | 待执行 |
| A-7 | 延期封顶 192h | 反复在窗口内出价 | `endTime` 不超过 `startTime+192h` | 待执行 |
| A-8 | 未到期不可结算 | `settle()` 在结束前调用 | revert `EA: auction not ended` | 待执行 |
| A-9 | 到期结算 | 到期后 settle() | 受益人收到**最高价 WJ**；emit `Settled` + `MintRequested` | 待执行 |
| A-10 | 幂等 | 再次 settle() | revert `EA: already settled` | 待执行 |
| A-11 | 无人出价结算 | 无 bid，到期 settle() | 不转账，emit `Settled(0,0,beneficiary)` | 待执行 |
| A-12 | 有出价不可撤销 | 有 bid 时 `cancel()` | revert `EA: has bids` | 待执行 |
| A-13 | 无出价可撤销 | 无 bid 时 owner 调 `cancel()`（经工厂） | `cancelled=true`，emit `Cancelled` | 待执行 |

### ★ B 组｜WJ 陷阱防护（本批重点）

| # | 用例 | 步骤 | 预期 | 状态 |
|---|---|---|---|---|
| **B-1** | **结算受益人 = `0x0` 时被拦截** | 构造时传 `beneficiary_=0` | **构造 revert** `EA: bad beneficiary` | 待执行 |
| **B-2** | **结算受益人 = WJ 合约时被拦截** | 构造时传 `beneficiary_=WJ_ADDRESS` | **构造 revert** `EA: bad beneficiary` | 待执行 |
| **B-3** | **`setBeneficiary` 传入 0/WJ 被拦截** | 结算前调 `setBeneficiary(0)` / `(WJ_ADDRESS)` | revert `EA: bad beneficiary` | 待执行 |
| **B-4** | **退款出口防陷阱**（若 `msg.sender` 异常无法触发，则验证函数体） | 代码审查：`withdrawRefund` 必经 `_wjSafeTransfer` | 该函数内**无绕过路径**（无直接 `safeTransfer`） | 待执行 |
| **B-5** | **结算出口防陷阱** | 代码审查：`settle` 转出必经 `_wjSafeTransfer` | 同上 | 待执行 |
| **B-6** | **工厂层防陷阱** | 工厂构造/setter 传 0 或 WJ_ADDRESS | revert `FAC: bad beneficiary` | 待执行 |

> **B-4/B-5 说明**：因 `_wjSafeTransfer` 的 `require` 已在**所有出口**前置，
> 实际运行中「to=0/WJ」的调用**不可能到达 `safeTransfer`**。若要做正向验证，
> 可临时改常量或在测试合约里直接调 `_wjSafeTransfer`（需继承）。

### C 组｜WJ6「同 tx 出价即成交」反证

| # | 用例 | 预期 | 状态 |
|---|---|---|---|
| C-1 | 同一 tx 内先 `bid` 再 `settle`（结束前） | `settle` revert（`ts < endTime`） | 待执行 |
| C-2 | 结束瞬间同 tx `bid` + `settle` | `bid` revert（`ts >= endTime`） | 待执行 |
| C-3 | 结论 | **两条件互补 ⇒ 不存在同时成立的时刻** | 逻辑证明 ✅ |

### D 组｜工厂

| # | 用例 | 预期 | 状态 |
|---|---|---|---|
| D-1 | 未授权地址 `createAuction` | revert `FAC: not approved` | 待执行 |
| D-2 | `createBatch` 传 4 个名字 | revert `FAC: batch size must be 5` | 待执行 |
| D-3 | 授权后可创建 | `approvedRequester[addr]=true` → 成功；`isAuction[新实例]=true`；`auctionCount` +1 | 待执行 |
| D-4 | `batchCount` 正确递增 | 创建一批后 `batchCount=1` | 待执行 |

---

## 五｜交付清单

| 文件 | 说明 |
|---|---|
| `jns-auction/contracts/EnglishAuction.sol` | 单品英式拍卖：168h + 防狙击（末10min→+10min，≤192h）+ 保留价 + pull 退款 + WJ 支付 |
| `jns-auction/contracts/JNSAuctionFactory.sol` | 工厂：审核后创建/登记、5 实例批量、授权名单、受益人治理 |
| `jns-auction/contracts/deps/Deps.sol` | 自包含依赖（IERC20/SafeERC20/Ownable/ReentrancyGuard），可一键替换为 OZ |
| 本文件 | 权限矩阵 + 风险点 + 自测用例（含 WJ 陷阱两项） |

**下一批（待本批确认后）**：`JNSMinterController`（接通中标者→域名铸造）、`SecondaryMarket`、`SubnameRegistry`。

**停机点（需 J-53 裁定）**：
1. **一级收入去向**（`defaultBeneficiary` 设谁）—— 建议创世金库多签
2. **是否允许 0 WJ 起拍**（`reservePrice=0`）
3. **中标者何时拿到域名**：本批只发 `MintRequested` 事件，仍需下一批 `JNSMinterController` 才真正铸造
