# 部署与多签执行清单

> 对象：JNS 拍卖系统四批合约（EnglishAuction + JNSAuctionFactory / SecondaryMarket + ClaimRegistry / SubnameRegistry / NotificationService）
> 生成：2026-09-14 · **只读核查版，未广播、未上链**
> ⚠️ **Jouleverse 无测试网**（主网 chainId **3666**，RPC JNS DAO 官方 RPC（授权端点，部署时配置）），且**四批合约均不可升级**
> ⇒ 部署参数必须事前逐项核对，**一次性正确**。

---

## 0. 常量

| 名称 | 地址 | 出处/性质 |
|---|---|---|
| WJ 代币 | `0x7fba9BB966189Db8C4fE33B7bf67Bfa24203c6AD` | 链上查明；**小写 c**；EIP-55 独立复核 PASS |
| JNS NFT | `0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89` | 链上查明 |
| JNS DAO 多签 | `0x4eF599b6E39D950D6Ddbd830fF5f95e06770C1B3` | 5860B **合约**、3 owners、`required()`==2、`JNS.owner()` |
| 审核人 J-0 | （EOA，地址见本地手册） | **EOA**（`eth_getCode`==`0x`）；core-contributors.md L37；EIP-55 独立复核 PASS |
| developer | **（J-53，EOA，地址见本地手册）** ✅ 已定 | 变更权仅其本人（自转让）；v1 休眠 |

---

## 1. 部署方式（必修1 · 已改正）

| 项 | 规定 |
|---|---|
| ① 部署广播方 | **普通 EOA 即可**（`DEPLOYER_ADDR`）—— 部署**不需要 owner 权限**；constructor 的 `owner` 参数**硬编码填多签** `0x4eF599…C1B3` |
| ② owner-only setter | 由**多签 2/3 流程**自行发交易执行；脚本**只产 calldata，不代劳** |
| ③ ⚠️ 常见误解 | 干跑时 `"Ownable: caller is not the owner"` 的成因是**【调用 setter 的地址】不是多签**，**与「谁部署」无关**（部署者是谁都不影响 setter 权限判定） |
| ④ 多签地址 | `0x4eF599…C1B3` 是**合约、无私钥** ⇒ 不可作 `--broadcast` 发送者；但这**不是** setter 失败的原因 |
| 脚本拆分 | **Part 1** `script/Deploy.s.sol`（仅部署）＋ **Part 2** `script/SetterCalldata.s.sol`（仅产 calldata） |

真部署命令：
```
DEPLOYER_ADDR=<持有私钥的EOA> forge script script/Deploy.s.sol:Deploy --broadcast --rpc-url $JNS_DAO_RPC_URL
```
（脚本内断言：`require(deployer != DAO_MULTISIG)`）

> ⚠️ **纠正**：本清单早前版本曾写「必须以多签身份广播」，**该表述是错的**——部署用普通 EOA 即可，问题只出在 setter 的调用方。

---

## 2. constructor 参数（五合约）

| # | 合约 | 参数 | 值 |
|---|---|---|---|
| 1 | `JNSAuctionFactory` | `owner_` | `0x4eF599…C1B3` |
| | | `auctionBeneficiary_` | `0x4eF599…C1B3` ① 一级受益 |
| 2 | `SecondaryMarket` | `owner_` | `0x4eF599…C1B3` |
| | | `tradeFeeRecipient_` | `0x4eF599…C1B3` ② 二级手续费 |
| 3 | `ClaimRegistry` | `owner_` | `0x4eF599…C1B3` |
| 4 | `SubnameRegistry` | `owner_` | `0x4eF599…C1B3` |
| | | `mintFeeRecipient_` | `0x4eF599…C1B3` ③ 子域名铸造费 |
| 5 | `NotificationService` | `owner_` | `0x4eF599…C1B3` |
| | | `developer_` | **`0x8b4846d72d…7147F`（J-53）** ✅ |
| | | `serviceFeeRecipient_` | `0x4eF599…C1B3` ④ 提醒服务费 |

**铁律**：owner **五处全部 = 多签**，不得为 EOA。
**铁律**：四受益地址**分别独立可配置**（禁用全局 feeRecipient），默认均填多签。

**部署顺序**（⚠️ 因工厂尺寸拆分，新增独立部署器，顺序不可调换）：

1. **部署 `EnglishAuctionDeployer`**（constructor 无参；`owner` = 部署广播者 msg.sender，仅用于一次性 `setFactory`）
2. **部署 `JNSAuctionFactory(deployer 地址)`**（constructor 第 3 参注入部署器地址，immutable）
3. **【关键】`Deployer.setFactory(factory)`** ← 一次性绑定，**漏调则拍卖创建永久失效**（工厂创建实例时 `deploy()` 因 `onlyFactory` 恒 revert）
4. `SecondaryMarket` → `ClaimRegistry` → `SubnameRegistry` → `NotificationService`

> **验收点**：第 ③ 步 `setFactory` 后，**立即校验 `Deployer.factory() == 真工厂地址`**（读链上值比对，非仅看 tx 成功），再**试创建一场拍卖验证成功**（`createAuctionFromRequest` 不 revert，实例正常部署）。若 `factory()` 不符（误绑到错误地址），此时尚无拍卖、无状态，**直接重部署 Deployer + 工厂重来**（成本低）；若漏调第 ③ 步，第 4 步各合约虽能部署，但工厂的拍卖创建能力已不可恢复（deployer 的 factory 不可重设）。

---

## 3. 修4 复核：**内联 vs 必须部署后调用**（逐项，无漏项）

### 3a. ✅ **已在 constructor / 默认值内联**（无需部署后调用）

| 项 | 内联值 | 代码位置 |
|---|---|---|
| `SecondaryMarket.tradeFeeRate` | `150`（1.5%） | `tradeFeeRate = DEFAULT_TRADE_FEE_BPS` |
| `SecondaryMarket.tradeFeeRecipient` | 多签 | constructor |
| `SubnameRegistry.mintFee` | `1e18`（1 WJ） | `mintFee = 1e18` |
| `SubnameRegistry.mintFeeRecipient` | 多签 | constructor |
| `SubnameRegistry.maxDepth` | `8` | `maxDepth = MAX_DEPTH_CEILING` |
| `NotificationService.perUseFee` | `5e16`（0.05 WJ） | `perUseFee = DEFAULT_PER_USE_FEE` |
| `NotificationService.feeCap` | `1e18`（1 WJ 上限） | `feeCap = 1e18` |
| `NotificationService.perUseFee` | `0`（不启用） | `perUseFee = 0` |
| `NotificationService.serviceFeeRecipient` | 多签 | constructor |
| `JNSAuctionFactory.signersRequired` | `1` | `signersRequired = 1` |
| `JNSAuctionFactory.auctionBeneficiary` | 多签 | constructor |

> 仍可调用 `setXxx` 以获得显式 emit 审计留痕（非必需）。

### 3b. 🔴 **必须部署后由多签执行**（constructor 不含）

| # | 合约 | 函数 | 参数 | 预期 emit |
|---|---|---|---|---|
| 1 | `JNSAuctionFactory` | `setReviewer` | `(0x5BF50F…B92076, true)` | `ReviewerSet(J0_EOA, true, ts)` |
| 2 | `ClaimRegistry` | `setRegistrar` | `(factory, true)` | `RegistrarSet(factory, true, ts)` |

> **仅此 2 条**必调。审核人**不得填多签**——否则链上 reviewer 是多签合约，无法映射到具体个人，「审核责任到人」落空。

### 3c. 🔶 **developer（J-53）——变更权仅其本人**

**【J-53 裁定·2026-09-15】部署时 constructor 直接填 J-53 本人地址**：
（`core-contributors.md` L90；链上实测 **EOA**；**≠ 多签**）

⇒ **无需 propose→accept 就位流程**（旧通道已停用）。

**变更权语义：沉默 = 否决**

| 项 | 说明 |
|---|---|
| owner/多签 发起更换 | 🚫 `proposeDeveloper` **恒 revert**（`DEV: owner cannot change developer`） |
| 唯一入口 | ✅ 当前 developer 自调 `transferDeveloperRole(newDev)` |
| 生效条件 | 满 7 天（`DEVELOPER_DELAY`）→ 被提名人主动 `acceptDeveloperRole()`，须落在 **[effectiveAt, expiryAt]**（+7 天窗口）内 |
| 逾期后果 | **自动作废**（绝非「到期自动生效」） |
| 反悔窗口 | 延迟期内当前 developer 可 `cancelDeveloperTransfer` 撤回 |
| **回答「J-53 沉默时，变更是否可能生效？」** | **否** |

**v1 状态：休眠**
- `perUseFee = 0` ⇒ `settleBatch` 金额恒为 0 ⇒ 逐笔跳过、不扣款
- 将来启用（由 J-53 本人发）：`setPerUseFee(5e16)`、`setServiceFeeRecipient(...)`

**已知代价（可接受）**：J-53 私钥丢失 ⇒ 角色锁死、无法更换；但 v1 提醒服务休眠 ⇒ **实际影响为 0**；其余四模块**不依赖 developer**。

> **`serviceFeeRecipient` 现状确认**：已是 `onlyDeveloper`（owner/多签/他人调用直接 `revert "Not developer"`）
> ⇒ **J-53 沉默即不可改**，无需改造。

### 3d. 跨批手工项（每次新建拍卖实例）
- `registry.setRegistrar(auction, true)` —— 否则该场拍卖无法登记待铸造任务

### 3e. 🔴 settleDelivery 前置条件链（多签交割动作，缺一即 revert）

> **🔴 硬性纪律（A1/R9-d 唯一有效缓解）**：交割一律走 `settleDelivery` 原子路径；**禁止多签违规直转 NFT 给赢家**。理由：实测证明正常 `settleDelivery` 后 escrow=Released、`claimTimeoutRefund` 直接 revert「EA: not in escrow」，无双花窗口；而绕过该路径会打开 R9-d/A1 双花。这是该风险唯一有效的缓解措施。

`settleDelivery()` 成功的前置条件（逐项，缺一项即 revert，顺序即 require 顺序）：

| # | 前置 | 缺此项的失败串 |
|---|---|---|
| 1 | `escrow == Held`（已 settle、未终态） | `EA: not in escrow` |
| 2 | 名字已铸造（`_nslookup != 0`） | `EA: not minted` |
| 3 | NFT 由多签持有（`ownerOf == JNS.owner()`） | `EA: not gov-held` |
| 4 | 多签 `unbind(tokenId)`（解除 CSBT 绑定） | `JNS: token is bound` |
| 5 | 多签 `setApprovalForAll(auction, true)`（授权拍卖合约代转） | `ERC721: not authorized` |
| 6 | 上述全满足 → `transferFrom` 成功 → NFT 转赢家 + WJ 付受益方 | — |

> **关键结论**：settleDelivery 的 require 仅验到「多签已持有」（第 3 项）；其**成功与否取决于多签是否已额外完成 unbind + setApprovalForAll（第 4、5 项）**。即「压在多签」状态能否原子交割，**依赖多签的配合动作**。

**与「禁止直转赢家」并列的铁律**：

- ❌ **禁止**多签绕过 settleDelivery，自行 `unbind` + `transferFrom` **直转赢家**——直转只交付了 NFT、未触发 WJ 放款，资金仍锁在 Held，破坏 DvP「一手交 NFT、一手付 WJ」的原子性（可致赢家既持 NFT 又无法自动收款）。
- ✅ **正确路径（预防·主）**：多签 claim 后【**立即**】`unbind(tokenId)` + `setApprovalForAll(auction, true)`（存入本合约托管授权，即刻可被任何人 settleDelivery 原子交割）——**措辞为「立即」而非「尽快」**：多签慢一步则进入「压在多签」态，只能等 45 天超时退款，且名字永久占死（见 §3g）。随后由**任何人**调 `settleDelivery()`，原子完成「NFT → 赢家、WJ → 受益方」，并 emit `DeliverySettled`。

### 3g. 🟡 名字占死三层兑底（流程侧，非链上自动；J-53 裁定 2026-09-24）

> **裁定**：**不在退款路径加任何名字释放/回收/burn 逻辑**。理由：合约无权从多签强制转走；会误伤正常场景（多签慢一步即作废）开出 griefing 面；前提是「多签违规」属可预防的运营事故。

| 层 | 动作 | 说明 |
|---|---|---|
| **① 预防（主）** | claim 后【**立即**】unbind + setApprovalForAll 存入托管 | 见 §3e 正确路径；措辞「立即」而非「尽快」 |
| **② 告警（链下监控）** | 监控两条：A.「多签已 claim 但 X 小时内未存入托管」；B.「已超时且退款成功」 | 任一触发即告警 J-53 |
| **③ 处置预案（非自动）** | 名字占死时由**治理层人工介入**（多签配合转回 / 治理决议另处理） | **链上无自动救济** |

---

## 4. ✅ 待 J-53 提供的占位：**已全部清零**

| 占位 | 状态 |
|---|---|
| ~~`<DEV_ADDR>`~~ | ✅ **已定**：developer（J-53，EOA，地址见本地手册）；**≠ 多签** |

**developer 硬约束（已满足）**：developer 必须与 owner 多签**不同地址**，否则**收益隔离失效**——
owner 可绕过 `onlyOwner`、经 developer 通道改 `serviceFeeRecipient`。
**实测：J-53 地址 ≠ 多签 `0x4eF599…C1B3`** ✅

`serviceFeeRecipient` 默认 = 多签（JNS DAO），**待 CGC 决议后**再由 developer（J-53）改。

**v1 休眠**：`perUseFee = 0` ⇒ 提醒服务 dormant，不产生任何扣费。费率上限为常量（单次 1 WJ / 月费 30 WJ）。

---

## 5. 部署前复核（勾选项）

- [x] `<DEV_ADDR>` **已定为 J-53 本人地址**，且 **≠ 多签** ✅
- [ ] `DEPLOYER_ADDR` 为持有私钥的 **EOA**（非多签）
- [ ] 五处 owner 均为多签，无 EOA
- [ ] 四受益地址均为 `0x4eF599…C1B3`
- [ ] §3b 两条 calldata 已备好，走多签 2/3 执行
- [ ] 清单 §3c 已在每场拍卖创建后补 `setRegistrar(auction, true)`
- [ ] WJ/JNS 常量地址**小写 c** 已核（`…24203c6AD`，非大写 C）
