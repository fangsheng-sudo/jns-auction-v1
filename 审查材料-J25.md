# JNS 域名拍卖系统 · 提交 J-25 审查材料

> **形态**：单文件自包含（形态 A）
> **生成**：2026-09-15｜**提交对象**：J-25（教链／@hmisty）
> **边界声明**：本材料为**只读产出**。未 commit / push / 部署 / 广播。**J-25 审查通过前不部署**（合约不可升级）。
> **测试基线**：**140 passed / 0 failed（13 suites）**；solc **0 warning / 0 error / 0 stack-too-deep**

---

## 目录

- [§0 阅读指引](#0-阅读指引)
- [§1 摘要](#1-摘要)
- [§2 设计说明（四模块 + 权限矩阵 + 状态机）](#2-设计说明)
- [§3 与 J-25 理念对照（核心）](#3-与-j-25-理念对照核心)
- [§4 测试与验证](#4-测试与验证)
- [§5 风险清单](#5-风险清单)
- [§6 待裁定事项表](#6-待裁定事项表)
- [§7 部署方案（仅报备，不执行）](#7-部署方案仅报备不执行)
- [§8 附录](#8-附录)
- [§9 JEEP-7 原文四问应答](#9-jeep-7-原文四问应答)

---

## §0 阅读指引

| 你是谁 | 建议先读 |
|---|---|
| 只想知道「与我理念是否冲突」 | **§1 → §3 → §6** |
| 关心技术正确性 | **§2 → §4 → §5** |
| 关心部署与风险 | **§5 → §7** |

**一句话**：这是一个把「JNS 主域名发放」从**人工专场拍卖**改为**链上自动拍卖**的系统，并为子域名、二级市场、提醒服务预留了接口。四模块共 6 个合约，140 条测试全部通过。

---

## §1 摘要

### 1.1 系统定位

| 项 | 内容 |
|---|---|
| 目标 | 把 JNS 主域名发放从「夜拍专场 + 人工主持 + 多签铸造」升级为**链上自动拍卖**，全程无人工主持 |
| 范围 | ① 一级拍卖（主域名）② 二级市场 ③ 子域名 ④ 提醒服务（v1 不启用） |
| 技术栈 | Solidity 0.8.0 / EVM istanbul / 无 proxy（不可升级） |
| 依赖 | 上游 JNS 主合约 `0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89`（V2）＋ WJ `0x7fba9BB966189Db8C4fE33B7bf67Bfa24203c6AD` |
| 编译 | `solc --evm-version istanbul`，optimizer 200；**0 error / 0 warning** |

### 1.2 与 J-25 / JEEP-7 的关系（**先说结论**）

> **本系统不是另起一套方案，而是实现了 J-25 在 `workspace#72` 中真正想要的目标：**
> 「**无需专人主持、零成本、自动拍卖**」。
> 同时把留痕载体从 **GitHub Issue**（可删改、可抵赖）**升级为链上**（不可篡改、可追责）。
> 详见 **§3.2**。

### 1.3 当前状态

| 项 | 状态 |
|---|---|
| 合约 | 6 个（EnglishAuction / JNSAuctionFactory / SecondaryMarket / ClaimRegistry / SubnameRegistry / NotificationService）+ 依赖层 Deps.sol |
| 测试 | **140 passed / 0 failed（13 suites）** |
| 静态检查 | solc **0 warning / 0 error / 0 stack-too-deep**；Foundry lint `note` 属风格建议，非编译器告警 |
| 审计修复 | 2×P0 + 3×P1 + 7×P2 全部修复并回归 |
| 待外部输入 | CGC 两项决议、**本材料 §6 的 #7/#10/#11~#14** |

### 1.4 材料构成

| 章节 | 内容 |
|---|---|
| §2 | 四模块设计 + 权限矩阵 + 状态机 |
| §3 | **与你的理念逐条对照（#1~#17）** ← 请重点看 |
| §4 | 测试与验证（含 gas 口径更正） |
| §5 | 风险清单（含不可回避的 R9） |
| §6 | **待你裁定 / 确认的事项** |
| §7 | 部署方案（报备，不执行） |
| §8 | 附录（地址、选择器、事件表、行数） |
| §9 | JEEP-7 原文四问应答 |

---

## §2 设计说明

### 2.0 模块总览

```
┌────────────────────────────────────────────────────────────────┐
│ 上游 JNS 主合约（V2，不可改，非本项目）                          │
│   0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89                    │
│   · ownerOf(tokenId) / _nslookup(name) / claim() / bind-unbind │
└───────────────┬────────────────────────────────────────────────┘
                │ 只读调用 + 多签负责 claim
    ┌───────────▼──────────────┐
    │ ① JNSAuctionFactory      │  申请→审核→建拍卖（每场独立实例）
    │   + EnglishAuction ×N    │  竞价→结算→放款→铸造
    └───────────┬──────────────┘
                │ 铸造完成后登记
    ┌───────────▼──────────────┐
    │ ② ClaimRegistry          │  【待铸造任务登记】(职责已收缩)
    └──────────────────────────┘

    ┌──────────────────────────┐
    │ ③ SecondaryMarket        │  域名 NFT 二级流通（挂单→竞价→成交）
    └──────────────────────────┘
    ┌──────────────────────────┐
    │ ④ SubnameRegistry        │  子域名 ERC-721（纯数字 label）
    └──────────────────────────┘
    ┌──────────────────────────┐
    │ ⑤ NotificationService    │  订阅+批量扣费（v1 不启用）
    └──────────────────────────┘
```

### 2.1 第 1 批｜一级拍卖（EnglishAuction + JNSAuctionFactory）

| 项 | 设计 |
|---|---|
| **审核留痕** | **链上锚定，弃用 GitHub Issue**。`reviewers[]` 地址数组，每个审核人用**自己钱包**签名调用 `approveRequest`；记录地址 + 时间戳；同一地址重复调用 revert |
| **审核门槛** | `signersRequired`（默认 1）；达门槛 → `status=Approved`，emit 事件 |
| **防自审** | **硬约束** `require(msg.sender != applicant)` |
| **起拍价** | = **申请人首次出价**（≥1 WJ 且必须整数 WJ，即 `% 1e18 == 0`） |
| **无流拍** | 无人加价 ⇒ **申请人兜底中标**（承担「必付」义务），emit `ApplicantWonInDefault` |
| **整数规则** | 仅约束拍卖出价/起拍价（二级市场同）；提醒服务**不适用** |
| **时间与防狙击** | 固定窗口 **168h 硬顶**；可**延长**不可暂停；上限 **192h** |
| **紧急叫停** | `emergencyCancel(reason)`（仅 owner），同步清 `highestBidder`，退款全走 pull |
| **超时退款** | **45 天**未铸造 ⇒ `claimTimeoutRefund()`（仅赢家本人） |
| **放款** | `releaseToDAO()` **去权限化**（任何人可触发），收款地址固定为创建时快照的 `auctionBeneficiary` |
| **白名单软校验** | `require(curOwner == highestBidder \|\| curOwner == IJNS(JNS_ADDRESS).owner(), "EA: minted to unexpected address")` |
| **工厂转发** | EA 的 **every onlyOwner** 函数都有工厂转发口（P0-1 开发规范） |

#### 2.1.1 审核留痕为何弃用 GitHub Issue

| 维度 | GitHub Issue（原提案） | 链上留痕（本设计） |
|---|---|---|
| 可删改性 | **可删改**（issue 可编辑/删除） | **不可篡改** |
| 可抵赖性 | 可否认（"我没说过"） | 地址签名，**不可否认** |
| 可追责 | 难（账号 ≠ 链上身份） | `reviewers[]` 地址可与 `core-contributors.md` 的 Core ID 映射 |
| 机器可读 | 需爬取 | 链上原生可读 |
| 成本 | 零 | 每笔审核 ≈ 25,163~120,044 gas |

#### 2.1.2 状态机

```
[提交] --submitRequest--> Pending
Pending --approveRequest×N--> Approved
Approved --createAuctionFromRequest--> Created(拍卖实例运行: Running)
Running --到期/达标 settle--> Settled
Running --emergencyCancel--> Cancelled
Running --无人出价且到 forceEnd--> Settled(申请人兜底中标)
Settled --45天未铸 claimTimeoutRefund--> Cancelled(退款)
Pending/Approved --cancelRequest/rejectRequest--> 释放 name 占用
```

#### 2.1.3 X-3 真漏洞修复（务必说明）

| 项 | 内容 |
|---|---|
| 漏洞 | 同一 name 可**并行多场申请** ⇒ 一方必被铸造占走，另一方 Held 资金因 `releaseToDAO` 恒 revert、旧退款又要求「未铸造」⇒ **永久锁死** |
| 修复① | `submitRequest` 加两道 require：`_nslookup(name)==0` + `requestOfName[name]==0` |
| 修复② | 新增 `cancelRequest` + `rejectRequest`，释放 name 占用 |
| 修复③ | `claimTimeoutRefund` 改为**安全阀**：判据从「未铸造」改为「`releaseToDAO` 必定 revert」 |
| 用例 | 新增 6 条，全 PASS |

### 2.2 第 2 批｜二级市场（SecondaryMarket + ClaimRegistry）

| 项 | 设计 |
|---|---|
| **硬约束（上游导致）** | JNS `unbind` 要求 `ownerOf==msg.sender`；`transferFrom` 带 `requireUnbound` ⇒ **二级成交必然两笔交易**（卖家先自行 `unbind`，再 list 拉 NFT），存在**抢转窗口** |
| **费率** | `tradeFeeRate` 默认 **1.5%**，归 `tradeFeeRecipient`（默认 JNS DAO） |
| **费率取法** | 采纳**甲案：成交时取当前值**（不挂单快照）——须 emit + 建议提前公告；⚠️ 须获 CGC 追认 |
| **取整方向** | 二级 `floor`、一级拍卖 `ceil` |
| **取消** | `cancelListing`（卖家本人或 owner 兜底）⇒ 所有出价者记入 `pendingReturns`，pull 自领（禁止批量转账，防 gas 爆炸/重入） |
| **ClaimRegistry** | 职责**已收缩为【待铸造任务登记】**；当前**零调用点** —— 去留待你裁定（§6 #U-5） |

### 2.3 第 3 批｜子域名（SubnameRegistry）

| 项 | 设计 |
|---|---|
| 形态 | **ERC-721**（与主域名一致，可交易） |
| 部署 | 独立部署（现有 JNS `_nslookup` 为 `mapping(string=>uint256)` **全串精确匹配、无层级**） |
| **label 规则** | **纯数字**：① 字符集仅 `0-9`（禁字母/连字符/空串）；② **禁前导零**（`01`/`001` 非法），`0` 本身允许 ⇒ 保证**唯一规范形式**；③ 长度 ≤ **19**（可安全转 uint64）；④ 原「敏感词风险由父持有者自担」声明**随之移除** |
| **深度** | `maxDepth = 5`；`MAX_DEPTH_CEILING = 5`（已与 fangsheng-sudo 建议 ≤5 对齐） |
| 铸造权限 | **仅父/根域名持有者**（`JNS.ownerOf(mainTokenId)==msg.sender` 或 `ownerOf(parentId)==msg.sender`） |
| 防伪造 | `require(IJNS(JNS_ADDRESS)._nslookup(parentFullName) == mainTokenId)` ⇒ 防「用自有 tokenId 配他人前缀」污染命名空间 |
| 再铸开关 | 父可设 `canMintSub`（子域名能否再铸子子域名） |
| 父控开关 | `setTransferable` / `renounceParentControl`（v1 **无收回权**） |
| **铸造费** | `mintFee = 1 WJ/个` → `mintFeeRecipient`（默认 JNS DAO）；**即时型**（每次读当前值，不快照） |
| 父域名卖出 | 已铸子域名 NFT **独立存在、不受影响** |
| 扩展位 | `reserved` 字段（v1 恒 0，为将来租期留位） |

#### 2.3.1 纯数字 label 的设计理由（与你的主张一致）

| 你的主张（JEEP-7） | 本实现的落实 |
|---|---|
| 「子域名限定为**纯数字**」 | ✅ `_validLabel` 仅允许 `0x30-0x39` |
| 「**纯数字 = 免审查**」 | ✅ 链上规则即可保证，无需人工审查 |
| 「靓号逻辑：短号/顺子/豹子号具有天然稀缺性」 | ✅ 链上无需额外逻辑，稀缺性天然存在 |
| 「纯数字是否足够？是否保留少数预留关键词？」 | ✅ **纯数字后不存在预留关键词问题**（全部数字串均受同一规则约束） |
| 29n 的评论「纯数字可读性差一点」 | ⚠️ 已通过**长度 ≤19 + 禁前导零**降低歧义；可读性代价换免审查 |

**新增的「禁前导零」理由**（本实现独有，请审阅）：
> 若允许前导零，则 `1` / `01` / `001` 是**三个不同 NFT** 但**显示相似** ⇒ 钓鱼混淆风险。
> 禁前导零后，**每个数值 ↔ label 一一对应**（唯一规范形式），从源头消除混淆。

### 2.4 第 4 批｜提醒服务（NotificationService，**v1 不启用**）

> **【本节为本轮新增说明，请重点审阅】**
> **v1 不启用（`perUseFee = 0` 休眠）；费率设计：单次 0.05 WJ（可调，上限 1 WJ，由 developer/J-53 独占调整）；月费封顶 30 WJ（常量，付满后当月继续提醒、不再扣费 = 包月制）；计价单位 WJ。**
> **developer 由 J-53 保留设置权**（占位地址，**≠ 多签**）；将来经 **J-53 本人自转让时启用**（`transferDeveloperRole`）。

| 项 | 设计 |
|---|---|
| 边界 | ❌ 不做前端弹窗 / ❌ 不做微信邮件推送 / ❌ 不做链下索引器；✅ **只提供「订阅 + 扣费 + 余额」链上能力** |
| 计费 | `perUseFee` 可配置（区间 0 ~ **1 WJ**，常量硬上限）；**整数原则不适用于此**；链下记账 + **周期批量结算** `settleBatch(periodStart, periodEnd, users[], counts[], detailHash)` |
| 资金模式 | 推荐**模式③ 预授权 + 按需拉取**（用户 `approve`，钱留自己钱包；可随时 `revoke`） |
| 安全要求 | S1~S5（预付费模式须满足；违反即打回） |
| 欠费上限 | `ArrearsCapped`（默认 5 WJ，可配置），达上限链下停服；**不整批回滚**（脏用户不砖化整批） |
| 防重放 | 周期重放 ⇒ **跳过 + emit `ChargeSkipped`**（而非 revert） |
| 权限硬隔离 | `serviceFeeRecipient` / `perUseFee` 归 **developer 独占自治**（perUseFee 0~1 WJ）；owner **只设 `setFeeCap` 上限**（仅可下调，下限 0.01 WJ） |
| 生效时点 | **即时型**：每次读当前值，不快照 |

### 2.5 权限矩阵全文

#### 2.5.1 三类角色边界

| 角色 | 定义 | 能做什么 | **不能**做什么 |
|---|---|---|---|
| **owner（治理）** | 部署后转入**治理多签**（JNS DAO） | 所有**治理参数与归属**：`auctionBeneficiary` / `tradeFeeRecipient` / `mintFeeRecipient` / DAO 侧三模块费率 / `setReviewer` / `setSignersRequired` / `emergencyCancel` / `releaseToDAO` / **`setFeeCap`** | **不得**改 `serviceFeeRecipient`；**不得**改 `perUseFee`；**不得**设超 `feeCap` 的费率；**不得**动用用户锁定资金 |
| **developer** | 可配置地址（**不写死**） | **自治**：`serviceFeeRecipient` + `perUseFee`（须 ≤ `feeCap` 与常量硬上限 `PER_USE_FEE_CAP` = 1 WJ）；可主动移交；7 天窗口内**可否决** owner 换人提议 | **不得**改 DAO 侧参数；**不得**超上限设费率；**不得**动用户资金 |
| **用户** | 申请人/审核人/出价者/卖家/买家/子域名铸造者/订阅者 | 自己的资金与资产 | 改治理参数；动**他人**资金 |

> **核心隔离**：`setServiceFeeRecipient` 加 `require(msg.sender == developer)` ⇒ **治理/owner 也不得更改开发者收益归属**。

#### 2.5.2 逐函数权限矩阵

**通用**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `transferOwnership(addr)` | ✅ | ❌ | ❌ | `OwnershipTransferred` |
| `renounceOwnership()` | 🚫 **已全局禁用**（覆写为 revert） | ❌ | ❌ | — |
| `transferDeveloperRole(newDev)` | ❌ | ✅ **仅当前 developer（自转让）** | ❌ | `DeveloperTransferProposed` |
| `acceptDeveloperRole()` | ❌ | ✅（仅被提名人，须在 [effectiveAt, expiryAt] 窗口内） | ❌ | `DeveloperChanged` |
| `cancelDeveloperTransfer()` | ❌ | ✅（仅当前 developer；延迟期内可撤） | ❌ | `DeveloperTransferCancelled` |
| `proposeDeveloper(addr)` | 🚫 **恒 revert**（已停用） | 🚫 | ❌ | — |
| `vetoDeveloper()` / `acceptDeveloper()` | 🚫 **已停用**（旧入口） | 🚫 | ❌ | — |

> **【J-53 裁定·2026-09-15】developer 变更权语义：沉默 = 否决**
> · owner/多签【无法发起】更换（`proposeDeveloper` 恒 revert）⇒ **多签沉默不可能导致变更**
> · 唯一入口 = 当前 developer 自调 `transferDeveloperRole`
> · Timelock = **未确认即不生效**（满 7 天后须被提名人主动确认；+7 天窗口内未确认 ⇒ **自动作废**，绝非“到期自动生效”）
> · **回答「J-53 沉默时，变更是否可能生效？」⇒ 否**
> · 代价：J-53 私钥丢失 ⇒ 角色锁死；但 v1 提醒服务休眠（`perUseFee = 0`）⇒ **影响为 0**
> · **`serviceFeeRecipient` 已是 `onlyDeveloper`**（owner/多签/他人直接 revert）⇒ J-53 沉默即不可改，无需改造

> **renounceOwnership 为何禁用（P2-4）**：本系统需**持续治理**（参数可调），弃权将使参数**永久不可调**。若确需冻结，用 `transferAuctionOwnership` 转黑洞地址。同时 `_transferOwnership` 已带 `require(newOwner != address(0))` ⇒ **无法绕道实现弃权**。

**JNSAuctionFactory**

| 函数 | owner | developer | 审核人 | 申请人 | 事件 |
|---|---|---|---|---|---|
| `submitRequest` | — | — | — | ✅ | `RequestSubmitted` |
| `approveRequest` | — | — | ✅（白名单，**硬约束 `require(msg.sender != applicant)`**） | ❌ | `RequestApproved` |
| `createAuctionFromRequest` | ✅ | ❌ | ✅（也可） | ✅（本人） | `AuctionCreated` |
| `cancelRequest` / `rejectRequest` | ✅ | ❌ | ✅ | ✅（本人） | `RequestCancelled` / `RequestRejected` |
| `releaseName(requestId)` | ✅（治理兜底） | ❌ | ❌ | ❌ | `NameReservationReleasedByGov` |
| `setReviewer` / `setSignersRequired` / `setAuctionBeneficiary` | ✅ | ❌ | ❌ | ❌ | 对应事件 |

> **`createAuctionFromRequest` 硬约束**：`require(status[requestId] == Approved)`。

**EnglishAuction**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `bid(amountWJ)` | — | — | ✅ 任意 | `Bid` / `Outbid` |
| `withdrawRefund()` | — | — | ✅ 本人 | `RefundClaimed` |
| `settle()` | — | — | ✅ 任意 | `Settled` / `ApplicantWonInDefault` / `MintRequested` |
| `releaseToDAO()` | ✅ | ❌ | ❌ | `WJReleasedToDAO`（前置 `minted==true`） |
| `claimTimeoutRefund()` | — | — | ✅ **仅赢家本人** | `TimeoutRefunded`（安全阀） |
| `markMinted(tokenId)` | ✅ | ❌ | ❌ | `MintCompleted` |
| `emergencyCancel(reason)` | ✅ | ❌ | ❌ | `EmergencyCancelled` |
| `extendTimeoutWindow()` | ✅ | ❌ | ❌ | `TimeoutWindowExtended` |
| `transferAuctionOwnership(addr)` | ✅ | ❌ | ❌ | `OwnershipTransferred` |

**SecondaryMarket**

| 函数 | owner | developer | 卖家 | 买家 | 事件 |
|---|---|---|---|---|---|
| `list(tokenId, price, hours)` | — | — | ✅ 本人 | ❌ | `Listed` |
| `bid(tokenId, amountWJ)` | — | — | — | ✅ | `Bid` / `Outbid` |
| `withdrawRefund(tokenId)` | — | — | — | ✅ 本人 | `RefundClaimed` |
| `settle(tokenId)` | — | — | — | ✅ 任意 | `Sold` / `FeePaid` |
| `cancelListing(tokenId)` | ✅（兜底） | ❌ | ✅ 本人（无出价时） | ❌ | `ListingCancelled` |
| `setTradeFeeRate` / `setTradeFeeRecipient` | ✅ | ❌ | ❌ | ❌ | `TradeFeeRateChanged` / `TradeFeeRecipientChanged` |

**SubnameRegistry**

| 函数 | owner | developer | 父域名持有者 | 子域名持有者 | 事件 |
|---|---|---|---|---|---|
| `mintSubname(mainTokenId, parentId, label, parentFullName)` | — | — | ✅ | — | `SubnameMinted` |
| `setTransferable(subId,bool)` | — | — | ✅（父，未放弃时） | ❌ | `TransferableSet` |
| `setCanMintSub(subId,bool)` | — | — | ✅（父，未放弃时） | ❌ | `CanMintSubSet` |
| `renounceParentControl(subId)` | — | — | ✅（父，**不可逆**） | ❌ | `ParentControlRenounced` |
| `setMintFee` / `setMintFeeRecipient` / `setMaxDepth` | ✅ | ❌ | ❌ | ❌ | `MintFeeChanged` / `MintFeeRecipientChanged` / `MaxDepthChanged` |

**NotificationService**

| 函数 | owner | developer | 用户 | 事件 |
|---|---|---|---|---|
| `settleBatch(periodStart,periodEnd,users[],counts[],detailHash)` | ❌ | ✅ **专属** | ❌ | `BatchSettled` / `ChargeSkipped` / `ArrearsCapped` / `MonthlyCapReached` |
| `setPerUseFee(uint256)` | ❌ | ✅ **独占自治**（区间 0 ~ 常量硬上限 `PER_USE_FEE_CAP` = 1 WJ） | ❌ | `PerUseFeeUpdated` |
| `setFeeCap(uint256)` | ✅ **多签安全刹车**（仅可下调，下限 0.01 WJ） | ❌ | ❌ | `FeeCapChanged` |
| `setServiceFeeRecipient(address)` | ❌ **禁止** | ✅ **专属** | ❌ | `ServiceFeeRecipientChanged` |
| `authorizeForPulling` / `revokeAuthorization` | — | — | ✅ 本人 | `AuthorizedForPulling` / `AuthorizationRevoked` |
| `deposit` / `withdrawBalance`（模式①） | — | — | ✅ 本人（提现**无条件**） | `Deposited` / `BalanceWithdrawn` |

### 2.6 收入归属与参数（**分模块独立，禁止全局 feeRecipient**）

| 模块 | 费率参数 | 归属参数 | 默认值 | 可改者 |
|---|---|---|---|---|
| `EnglishAuction` | —（全额） | `auctionBeneficiary` | JNS DAO 多签 | **owner** |
| `SecondaryMarket` | `tradeFeeRate`（默认 **1.5%**） | `tradeFeeRecipient` | JNS DAO 多签 | **owner** |
| `SubnameRegistry` | `mintFee = 1 WJ/个` | `mintFeeRecipient` | JNS DAO 多签 | **owner** |
| `NotificationService` | `perUseFee`（**developer 独占自治**，0~1 WJ）；`MONTHLY_FEE_CAP` = 30 WJ（常量） | `serviceFeeRecipient` | JNS DAO（先）→ CGC 决议后切 | **仅 developer**；owner 只设 `feeCap`（仅可下调） |

**JNS DAO 地址处理**：合约**不得写死**任何归属地址 ⇒ 全部**可配置参数**，部署后填入；每个 setter 必须**权限保护 + emit 事件**。

### 2.7 全局技术要求 T1~T8 落实

| # | 要求 | 落实 |
|---|---|---|
| T1 | **WJ 陷阱防护**：所有对外 WJ 转账必经 `_wjSafeTransfer()` | 各合约**唯一出口** `_wjSafeTransfer()`；退款/结算/手续费/铸造费/批量扣费全部经它；`beneficiary`/`recipient` 类 setter **预校验**（非 0、非 WJ） |
| T2 | WJ/JNS 地址**独立复核** | ✅ 已完成（EIP-55 自实现 keccak256 校验；两常量加醒目区分注释） |
| T3 | **event 写全** | 全事件表（§8.3），均带 `nameOrTokenId / addresses / amountWJ / block.timestamp` |
| T4 | 合约**不可升级** | 无 proxy / UUPS / `initialize`；修复靠新模块交付 |
| T5 | `IERC20` + `SafeERC20` | 统一依赖层；批量场景备**不 revert** 变体 |
| T6 | v1 **不做合约内 wrap** | 全合约无 `deposit`/`wrap`；文档引导「先 wrap 成 WJ，再 approve+出价」 |
| T7 | **避免同 tx 出价即成交** | **条件互补反证**：`bid` 要求 `ts < endTime`；`settle` 要求 `ts >= endTime` ⇒ 不存在同时成立的时刻 |
| T8 | 编译 `--evm-version istanbul`，**0 error / 0 warning** | ✅ 实测达成 |

### 2.8 WJ 陷阱（重要，供审阅者理解）

> **真实 WJ 的 `transfer`/`transferFrom`，若 `to == address(0)` 或 `to == WJ 自身`，不是转账，而是「烧掉 WJ 并把 J 退给 `msg.sender`」。**

因此本系统所有对外 WJ 转账**只有一个出口** `_wjSafeTransfer()`，首行 `require(to != address(0) && to != WJ_ADDRESS)`，并附注释说明原因。所有 `beneficiary`/`recipient` 类 setter 均**预校验**。

---

## §3 与 J-25 理念对照（核心）

> **方法论声明**：本章只提炼**有原文支撑**的主张；无原文支撑者一律标 **「未提及」**，**不推断、不脑补**。
> 你的原文出处：`jips#7`（JEEP-7，2026-07-21）、`workspace#72`（2025-12-22 / 2026-02-06）、`open-meetings#77`（2026-07-26 发言总结）、`jnsdao/jnsdao` 官方 docs 四篇。

### 3.1 一致性（✅ 已对齐，共 8 项）

| # | 你的主张（出处） | 本系统实现 | 状态 |
|---|---|---|---|
| 1 | 域名**绑定态=灵魂绑定（不可转移）**，解绑后恢复普通 NFT（`about-jns.md`） | 二级市场须先 `unbind`，故**必然两笔交易**；已按 JNS 语义处理 | ✅ 一致 |
| 2 | 主域名发放须**前向合规审查**（`jns-dao.md`：core 暂留 mint 权限「避免政治、文化等方面的风险」；`workspace#72`：「前向合规审查：core 找人出来先担当一下就可以解决」） | **审核人白名单 + 会签**，全链上留痕 | ✅ 一致 |
| 3 | **无过期回收**：「一次拍卖、**终生持有**」「**不需要 expiry 逻辑，不需要 renew 功能**」（`jips#7` 评论③） | 无回收、无 expire；完全依赖上游 JNS 语义 | ✅ 一致 |
| 4 | **WJ 计价**（`workspace#72`：「我愿意付出的**WJ**」「向 jnsdao 多签转入对应数量 WJ」） | 全系统以 **WJ** 计价；J 仅作 gas | ✅ 一致 |
| 5 | **子域名纯数字 + 免审查**（`jips#7` 正文） | **已改为纯数字**（禁前导零、≤19 位），链上规则保证免审查 | ✅ **已完全一致** |
| 6 | **靓号稀缺性**：「短号/顺子/豹子号具有天然稀缺性和交易价值」（`jips#7`） | 纯数字后天然存在；链上**无需额外逻辑**，市场自然形成 | ✅ 一致 |
| 7 | **预留关键词问题**（`jips#7` 开放问题） | 纯数字 ⇒ **不存在预留关键词问题** | ✅ 无需处理 |
| 8 | **层级 ≤5 级**（fangsheng-sudo 在 `jips#7` 评论建议，你已采纳） | **已改 `maxDepth = 5`** | ✅ **已一致** |

### 3.2 🔴 留痕载体：GitHub Issue vs 链上（**需你确认**）

**你的原文**（`workspace#72`，2025-12-22）：

> 「几个问题解决好就可以进行：…3. **拍卖工作：这个工作目前无人主持。开发拍卖系统什么的存在一系列障碍。可能需要零成本的、无需专人主持的、创造性的新方法**」
>
> 「比如我想到一个比较轻量级的办法，就是利用 github issue 的功能进行竞拍式域名注册：…把每个域名的注册需求看成是一个待解决的 github issue…规定一个时间比如一周（7天）…**最新最高出价…发出后超过一周没有更新更高的出价，则视为中拍**…最后，repo 维护人定期…收拢中拍的 issue，关闭 issue，整理列表，写入 jnsdao repo 网站存档，并提交多签人铸造和发放。」
>
> 「**我写的比较粗线条。** 实际做过 jnsdao 拍卖的朋友比如 @koant-bit 可以理一理有什么遗漏的、不当的细节问题，评估一下可行性。」
>
> 「P.S. 合规审查、repo维护人、多签人等都有一些支撑性的工作量，可以暂由 core powh 予以工作量激励。」

**出处**：<https://github.com/Jouleverse/workspace/issues/72>

#### 本方案说明

> **我们并非违背你的方案，而是实现了它真正想要的目标。**
>
> 你的方案有明确的**前提**：当时「**拍卖工作无人主持**」，因此需要「**零成本、无需专人主持**」的**退而求其次**办法；你自评「写得比较粗线条」，并请 `@koant-bit` 评估可行性。
>
> **现在前提已不复存在**——链上自动拍卖系统已建成并测试通过（140/140）：无人主持、自动竞拍、自动结算。
>
> 我们在此基础上做了**一处升级**：把**留痕载体从 GitHub Issue（可删改、可抵赖）升级为链上**（`reviewers[]` 地址 + 时间戳，不可篡改、可追责）。
>
> 换言之：**目标一致（无人主持、自动拍卖、成本极低），载体更强（链上 vs GitHub）。**

#### 逐项对照

| 维度 | 你的 GitHub Issue 方案 | 本系统 | 评价 |
|---|---|---|---|
| 主持成本 | 需 repo 维护人定期收拢 | **零人工**（全自动） | ✅ 实现你的目标 |
| 竞拍机制 | 7 天倒计时，加价**重置**倒计时 | 固定 168h 硬顶 + 可延长（≤192h） | 🟡 语义接近、机制不同（见 #13/#16） |
| 中拍判定 | 「最后出价 + 一周无更新」 | `settle()` 到期触发（任何人可调） | ✅ 等效 |
| 留痕载体 | **GitHub Issue（可删改）** | **链上 `reviewers[]`（不可篡改）** | ⬆️ **升级** |
| 支付方式 | 「附上向多签转 WJ 的交易链接」（你自问：提前转避免流拍 vs 退款量大增） | **合约托管**（提交流程内 `transferFrom` 拉取） | ✅ 解决你所指的「退款工作量大」痛点 |
| 铸造 | 「提交多签人铸造和发放」 | 铸造请求链上登记 → **多签 claim → 转赢家** | ✅ 保留多签环节（合规审查需要） |
| 合规审查 | 「core 找人出来先担当一下」 | **审核人白名单 + 会签（链上）** | ✅ 一致且更强 |

> **待你确认**：是否接受「留痕载体由 GitHub 升级为链上」这一改动？
> 若你坚持 GitHub Issue 作为**主载体**，我们可将链上留痕作为**并行记录**（两者并存），但需你明确「以哪个为准」。

### 3.3 🟡 费用边界：子域名 1 WJ 铸造费是否归 DAO（**待裁定**）

**你的原文**（`jips#7` 评论③）：

> 「**子域名**：运营商交付给会员时，**是否收费、收多少、周期性还是一次性——完全由运营商自行决定，JNS DAO 不介入、不控制**。链上只记录子域名的存在和绑定关系。」
>
> 「简单说：**JNS DAO 只收一道门票钱，里面怎么玩是运营商的自由。**」

**出处**：<https://github.com/Jouleverse/jips/issues/7>

#### 本方案理解（两笔钱、两个层次）

| 层次 | 内容 | 归属 | 依据 |
|---|---|---|---|
| ① **链上铸造费** | `mintFee = 1 WJ / 个` | **JNS DAO** | 对应**链上存储/gas 成本**（`mintSubname` 确会 SSTORE 新节点、铸 ERC-721） |
| ② **运营商向会员收多少** | 完全由运营商自定 | **运营商** | 与你的「里面怎么玩是运营商的自由」**完全一致**；DAO 不介入、不控制 |

> **两层并不矛盾**：① 是「对链上资源的成本回收」，② 是「运营商对会员的商业行为」。
> 「只收一道门票钱」= 主域名拍卖；子域名的 **1 WJ** 更接近「链上记账成本」，可类比快递费而非门票。

#### 若你的本意是「连 1 WJ 也由运营商定」⇒ 改动量评估

| 项 | 内容 |
|---|---|
| 改动点 | `SubnameRegistry` 新增 `mapping(uint256 => uint256) operatorFee`（按父域名 tokenId 计） |
| 新增函数 | `setOperatorFee(uint256 parentTokenId, uint256 fee)`（仅该父域名持有者） |
| 修改 | `mintSubname` 改读 `operatorFee[mainTokenId]`（默认 0） |
| 新增用例 | 3~4 条（父自设费、默认 0、非父不可设、即时生效） |
| **工作量** | **约半天**；**不影响其他三模块**（子域名模块自包含） |
| 风险 | 运营商可设任意价 ⇒ 若担心乱收费，可加**上限由 owner 设**（同 `feeCap` 思路） |

> **请裁定**：① 维持「1 WJ 归 DAO」；② 改为「父持有者自设」；③ 其他（请明示）。

### 3.4 🟡 未提及项（**请补充意见**）

| # | 事项 | 你的原文 | 本系统 | 请指示 |
|---|---|---|---|---|
| **#11** | **起拍价 = 申请人首次出价** | **未提及** | 起拍价 = 申请人首次出价（≥1 WJ 整数）；无人加价则申请人**兜底中标**（承担「必付」义务） | 是否接受？「必付」经济含义是否合理？ |
| **#12** | **加价幅度 / 5% 规则** | **未见原文**。官方 `jns-auction.md` 仅写「预约加价皆为整数」；细则指向语雀外站 `yuque.com/koant/pba3ib/tm7olavoxlr6qya5`（**无法直连**） | `minIncrement`：默认 1 WJ 起，可按起拍价比例 | **请提供语雀原文或确认无 5% 规则** |
| **#13** | **拍卖时长 / 延时规则** | **未见原文**。「7 天 + 加价重置倒计时」是你在 `workspace#72` 的**个人提案**，非官方既定规则 | 固定 **168h 硬顶** + 可延长不可暂停（**192h 上限**） | 是否接受固定窗口？或需改为「加价重置」？ |
| **#14** | **主域名二级流通** | **未提及**（你仅对**子域名**表态「靓号可通过市场转售，形成二级交易」，`jips#7`；**主域名流通未表态**） | `SecondaryMarket` **已实现**，但**未裁定开放** | **请就主域名二级流通表态** |

### 3.5 ✅ mintFee 支付主体（**已裁定，告知**）

> `mintSubname` 的调用权限**仅限父/根域名持有者**（`SR: not main domain owner` / `SR: not parent owner` / `SR: not owner`）⇒ **`msg.sender` 必为父域名持有者** ⇒ 铸造费自然由**父域名持有者**支付，**非会员、非子域名接收者**。
>
> 这与你的设定「**主域名持有人自动成为运营商，可自主向社区成员分配数字子域名**」（`jips#7`）**一致**。

| 项 | 内容 |
|---|---|
| 支付主体 | **根/父域名持有者（铸造发起者）** |
| 非支付主体 | 会员 / 子域名接收者 |
| 依据 | 函数权限设计（`msg.sender` 必为父） |
| **附注** | v1 的 `mintSubname` 只能铸给 `msg.sender` **自己**（无 `to` 参数）⇒ 若将来需「**运营商代会员铸造**」，需增加 `to` 参数（新函数或新版本）——**请确认是否需要** |

### 3.6 架构差异：#7 独立部署 vs 单 Registry（**冲突已减轻**）

| 项 | 你的原文 | 本系统 | 评价 |
|---|---|---|---|
| 架构 | 「**operator 独立部署子域名合约**（或通过主合约的 mapping 能力），持有人自主 mint 纯数字子域名」；评论②补充「ENS 的核心是**一个 Registry 合约**…**不需要部署任何新合约**」（`jips#7`） | **独立部署 `SubnameRegistry`**（DAO 级统一部署，非每个主域名一个） | 🟢 **冲突减轻** |

**结论**：你在评论①中把「**operator 独立部署子域名合约**」列为**可选架构之一** ⇒ 独立部署**非理念违背**。本系统选择独立部署是**被迫 + 省 gas**：
- **被迫**：V2 JNS **无 `setSubnodeOwner`**（`_nslookup` 是全串精确匹配、无层级），无法在 JNS 主合约内建层级；
- **省 gas**：统一部署一个 registry，避免每个主域名持有者各部署一份。

**定位**：**V2 过渡方案**；V3 按 JEEP-7 重构（若 V3 主合约实现层级，本模块可退役或改造）。

### 3.7 与 JEEP-7 另两个方向的关系

| 你的方向 | 对本系统影响 |
|---|---|
| **1️⃣ 多类型绑定**（多链地址 + 社交账号，使 JNS 成为「全网身份聚合器」） | **不直接耦合**。但⚠️ 本系统 `releaseToDAO` 白名单校验依赖 `IJNS._nslookup`；V3 若重构主合约则**接口可能变**（需重新适配）。 |
| **2️⃣ 治理 deadlock 修复**（quorum 自适应 / 委托投票 / 紧急熔断） | **不冲突**。属 CGC 治理合约范畴，与拍卖系统独立。 |

> ⚠️ **JEEP-7 开放问题含「拍卖机制是否一并调整？」** ⇒ 本系统存在 V3 被调整的可能。**本材料即为该问题的提案**，请你裁示。

### 3.8 对照总表（#1~#17）

| # | 你的主张（出处） | 本系统 | 冲突 | 建议 |
|---|---|---|---|---|
| 1 | 绑定态=灵魂绑定（`about-jns.md`） | 须先 `unbind` ⇒ 必然两笔交易 | ✅ | 保持 |
| 2 | 主域名前向合规审查（`jns-dao.md`/`#72`） | 审核人白名单 + 会签 | ✅ | 保持 |
| 3 | 无 expiry、终生持有（`jips#7`） | 无回收 | ✅ | 保持 |
| 4 | WJ 计价（`#72`） | 全系统 WJ | ✅ | 保持 |
| 5 | 纯数字免审查（`jips#7`） | **已改纯数字** | ✅ | 保持 |
| 6 | 架构：单 Registry vs 独立部署（`jips#7`） | 独立部署 `SubnameRegistry` | 🟢 减轻 | **依据**：评论①「operator 独立部署」为自列可选；本系统**被迫 + 省 gas**，定位 V2 过渡 |
| 7 | 子域名费用：DAO 不介入（`jips#7`） | 1 WJ 归 DAO + 运营商自定会员价 | 🟡 **张力仍在** | **两笔钱两层**（§3.3）；若本意为「全由运营商定」⇒ 改动量约半天 |
| 8 | 靓号稀缺性 | 天然存在 | ✅ | 链上无需额外逻辑 |
| 9 | 预留关键词（`jips#7`） | — | ✅ | 纯数字 ⇒ 无此问题 |
| 10 | **GitHub Issue 竞拍**（`#72`） | **链上留痕** `reviewers[]` | 🔴 **理念分歧** | **见 §3.2**（非违背，是实现目标 + 载体升级） |
| 11 | 起拍价=申请人首出 | **未提及** | 🟡 | 请补充意见 |
| 12 | 加价幅度/5% | **未见原文**（语雀外站） | 🟡 | 请提供原文或确认 |
| 13 | 拍卖时长/延时 | **未见原文** | 🟡 | 请确认固定窗口 vs 加价重置 |
| 14 | 主域名二级流通 | **未提及** | 🟡 | 请表态 |
| 15 | 7 天窗口（`#72`） | 168h/192h | ✅ | 保持 |
| 16 | 加价**重置**倒计时（`#72`） | 固定硬顶 + 可延长 | 🟡 | 见 #13 |
| 17 | 层级 ≤5 级（JEEP-7 评论） | **已改 `maxDepth=5`** | ✅ | 保持 |

**冲突分级**：
- 🔴 **需裁定后再定，否则可能返工**：#10（留痕载体）
- 🟡 **需确认**：#7（费用边界）、#11~#14（未提及项）、#16
- 🟢 **已减轻/一致**：#1~#6、#8、#9、#15、#17

---

## §4 测试与验证

### 4.1 测试总览

| 项 | 结果 |
|---|---|
| 测试用例 | **140 passed / 0 failed / 0 skipped** |
| 测试套件 | **13 suites** |
| 编译 | solc 0.8.0 / istanbul / optimizer 200 ⇒ **0 warning / 0 error / 0 stack-too-deep** |
| lint | Foundry `note[...]` 风格建议（非编译器告警） |

| 套件 | 用例数 | 覆盖 |
|---|---|---|
| `Smoke.t.sol` | 2 | 冒烟 |
| `TestAB.t.sol` | 11 | A/B 组基本流程 |
| `TestCD.t.sol` | 13 | C/D 组 |
| `TestEF.t.sol` | 27 | E/F 组（含子域名 19 条：纯数字 7 + maxDepth 2 + 原有 10） |
| `TestGH.t.sol` | 14 | G/H 组 |
| `TestDevPath.t.sol` | 5 | developer == 0 就位路径 |
| `TestSupplement.t.sol` | 25 | 补测（ERC-677 / 192h / 兜底中标 / X-3） |
| `TestP0Fix.t.sol` | 16 | P0 修复回归 |
| `TestP2Coverage.t.sol` | 11 | P2 补覆盖 |
| 其余 | 2 | Base/Smoke 辅助 |

### 4.2 关键用例分组

| 组 | 内容 | 结果 |
|---|---|---|
| 基本拍卖 | 提交→审核→建拍→竞价→结算→放款 | ✅ |
| **WJ 陷阱防护** | `to`=0 / `to`=WJ 的拦截 | ✅ |
| **防伪造** | `parentFullName` 与 `mainTokenId` 不匹配 revert | ✅ |
| **X-3 同名占位** | 6 条（含安全阀 2 条） | ✅ |
| **纯数字 label** | 7 条（合法/非数字/前导零/空串/超长/重复/无混淆面） | ✅ |
| **maxDepth=5** | 2 条（边界 + setter 上限） | ✅ |
| ERC-677 回调 | 9 条 | ✅ |
| 192h 硬顶 | 含 144 次连打触顶 | ✅ |
| 兜底中标 + pull 退款 | ✅ |
| developer 就位路径 | 5 条（propose→7天→accept→veto） | ✅ |

### 4.3 gas 表（含**口径更正**）

> **⚠️ 重要更正**：此前流传的「mintSubname 由 229,054 涨至 373,049 / 736,544（涨 63%~221%）」**是口径混淆导致的假象，实际并未上涨**。现将更正结论写入本材料。

#### 4.3.1 口径澄清

| 数字 | 口径 | 含义 |
|---|---|---|
| **229,054** | `forge test --gas-report` **函数级** Max | 仅一次 `mintSubname` 调用 |
| **373,049 / 736,544** | `forge test -vv` **测试级** gas | **含 `_claimTo` 完整「拍卖→铸主域名」流程 ≈145,893 gas** |

#### 4.3.2 A/B 同口径实测（纯数字改动前 vs 后）

| 探针 | 旧（字符串 label） | 新（纯数字 label） | 差 |
|---|---|---|---|
| `_claimTo` 单跑 | 145,893 | 145,893 | **0** |
| +`"888"` | 355,712 | 355,204 | **−508** |
| +19 位数字 | 362,329 | 359,917 | **−2,412** |
| 连铸 1 个 | 354,954 | 354,684 | **−270** |
| 连铸 2 个 | 533,852 | 533,312 | **−540** |
| 连铸 3 个 | 711,304 | 710,494 | **−810** |
| 递归 3 层 | 750,739 | 749,929 | **−810** |

⇒ **纯数字改动使 gas 略微【下降】0.1%~0.7%**。
原因：去掉了 `'-'` 比较与首尾字符判断；长度上限由 63 降至 19 使字符串更短。
**并非** `_validLabel` 循环变多（仍是**单次逐字符循环**，仅比较边界由**三区间收窄为一区间**）；**无**新增 uint 转换或唯一性检查（`_idOfName` / `_join` 与旧版逻辑相同）。

#### 4.3.3 官方 gas-report 同口径对比

| 版本 | Min | Avg | Max | Calls |
|---|---|---|---|---|
| 旧（字符串 label） | 28,957 | 148,867 | 229,054 | 21 |
| **新（纯数字 label）** | 28,957 | **141,114** | **233,041** | 43 |

⇒ Max **+1.7%**（新增 19 位长 label 用例抬高上限），Avg **−5.2%**（大量短号用例）。

#### 4.3.4 关键路径 gas 峰值

| 路径 | 合约·函数 | Avg | **Max** |
|---|---|---|---|
| 出价 | `EnglishAuction.bid` | 85,668 | **102,511** |
| 结算 | `EnglishAuction.settle` | 76,885 | **83,135** |
| 放款 | `EnglishAuction.releaseToDAO` | 67,796 | **83,999** |
| 超时退款 | `EnglishAuction.claimTimeoutRefund` | 41,789 | **60,755** |
| pull 退款 | `EnglishAuction.withdrawRefund` | 30,161 | **35,642** |
| **铸造子域名** | `SubnameRegistry.mintSubname` | **141,114** | **233,041** |
| 周期结算 | `NotificationService.settleBatch` | 152,791 | **349,023** |
| **建拍卖** | `JNSAuctionFactory.createAuctionFromRequest` | 1,926,606 | **2,045,847** |

> `createAuctionFromRequest` 峰值 ≈ **2.05M**：内含「部署一个 EnglishAuction 实例 + 拉取起拍价」——**每场拍卖的最大单笔成本**。

#### 4.3.5 批量铸边际成本（「无限创建」场景）

| 项 | gas |
|---|---|
| 首个（含冷槽位） | ≈208,791 |
| **后续边际** | **≈177–179k/个**（实测 178,628 / 177,182） |
| 批量 10 个 | ≈ **1.8M** |

#### 4.3.6 可优化点（**本轮不改**，留待你意见）

| # | 优化点 | 预计降幅 | 风险 |
|---|---|---|---|
| **O-1** | **批量铸造** `mintSubnameBatch(labels[])` | 每多铸省 **20–30%** | 新增外部函数，需新用例 |
| **O-3** | 删 `mainOwner` 快照位（仅留痕，无逻辑用途） | **~20k/铸** | 损失链上留痕（event 已覆盖） |
| **O-4** | 删 `reserved` 扩展位（v1 恒 0） | **~20k/铸** | 失去未来扩展位 |
| **O-2** | 不存 `fullName`（只存 label + 父 id） | 深层显著 | **与「留痕」目标冲突，不建议** |

> O-3 + O-4 合计 **~40k/铸（降约 19%）**，收益确定；但**合约不可升级** ⇒ 建议**留待你审查意见后一并决定**。

---

## §5 风险清单

> 等级：🔴 高 / 🟡 中 / 🟢 低

### 5.1 R9 多签相关（最高优先）

| # | 风险 | 等级 | 现状 / 处置 |
|---|---|---|---|
| **R9-a** | **多签在铸造前失效** ⇒ `JNS.claim` 仅 owner（多签）可调 ⇒ **域名铸不出** | 🔴 | 无链上解：45 天超时**全额退赢家**兜底；**系统可用性依赖 2/3 多签存活** |
| **R9-b** | **多签「铸而不转」**（claim 后未把 NFT 转给赢家） | 🔴 | `releaseToDAO` **软校验放行**（`ownerOf == JNS.owner()`）⇒ 款项照付多签，**赢家两头空**。❗**赢家无链上救济**，依赖多签诚信 |
| **R9-c** | 多签**铸给无关第三方** | 🟡 | `releaseToDAO` 白名单校验 **revert**（资金停 Held）→ 治理纠正后重触发；长期不可纠正走 `extendTimeoutWindow` + 特别决议 |

> **R9 结论（须写入风险公示）**：**多签铸而不转时赢家无链上救济，依赖多签诚信。**

### 5.2 Mock 与真链差异

| # | 差异 | 等级 | 处置 |
|---|---|---|---|
| M-1 | MockJNS 非真实 JNS 全量复刻 | 🟡 | 试点用**真实 JNS** 验证 `_nslookup != 0` 与 `ownerOf` |
| M-2 | Mock 用 `etch` 落址，未跑构造函数 | 🟢 | 仅测试装置问题 |
| M-3 | **无测试网**（chainId 3666 无 testnet） | 🔴 | 金丝雀试点即「主网小规模预演」 |
| M-4 | 本地 gas ≠ 主网 gas（主网 2.5 gwei） | 🟢 | 按主网实测价估算，留余量 |
| M-5 | 真实 WJ 的 `onTokenApproval` selector 实测**不存在**（字节码 0 次），而旧文档记「有」 | 🟡 | 文档已按字节码订正；本设计不使用该路径 |
| M-6 | `claim` 铸给多签后再转赢家需**先 `unbind`** | 🟢 | runbook 已写明两笔交易 |

### 5.3 未真实验证项

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| T-1 | **45 天超时退款仅本地 `vm.warp` 验证** | 🟡 | 主网首次真实触发需等待；建议届时专人复核 |
| T-2 | `extendTimeoutWindow` 主网未真实触发 | 🟢 | 本地通过 |
| T-3 | 192h 封顶路径本地已验证 | 🟢 | 已 PASS |

### 5.4 ERC-677 回调

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| E-1 | 真实 WJ 含 `*AndCall` ⇒ 若有人与拍卖合约交互 | 🟢 | 实测：拍卖合约**未实现钩子** ⇒ 整笔**原子回滚** |
| E-2 | 回调内重入/状态错乱 | 🟢 | 实测状态一致；另用 `nonReentrant` |
| E-3 | 本设计**内部一律只用 `transferFrom`** ⇒ 全程零回调 | 🟢 | 已 PASS |

### 5.5 其他残余风险

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| X-1 | **合约不可升级 + 无测试网** ⇒ 主网首秀即定型 | 🔴 | 金丝雀试点前置；中止预案分 G0~G6 决策门 |
| X-2 | `settle()` 依赖外部调用，无人调则挂起 | 🟡 | 设计为任何人可调；可另配链下 bot |
| X-3 | ~~同名可重复申请~~ ⇒ ✅ **已修**（真漏洞，含 6 用例） | — | 见 §2.1.3 |
| X-4 | 二级市场挂拍即托管 = 必然两笔交易，**抢转窗口** | 🟡 | 已按设计；文档明示 |
| X-5 | 申请人兜底中标是否合理 | 🟢 | 已按设计 |
| X-6 | 手续费取整方向（二级 `floor`、拍卖 `ceil`） | 🟢 | 已实现并测试 |
| X-7 | `reviewRef` 仅作锚点，合约不验证审核内容 | 🟢 | 按设计（审核链下，链上只留哈希） |
| X-8 | Foundry lint `note` 条数（风格建议） | 🟢 | **非编译器 warning**；solc Warning = 0 |
| X-9 | 多签余额需足够支付 gas | 🟢 | 实测 **0.320001 J** ≥ 0.05 J ✅ |

### 5.6 CGC 待决议 + 未定项

| # | 事项 | 当前默认 |
|---|---|---|
| C-1 | 提醒服务费 `serviceFeeRecipient` 归属 | 默认先设 JNS DAO 多签；待 CGC 决议后由 developer 改 |
| C-2 | 二级市场 `tradeFeeRate` 取法 | **已采纳甲案**（成交时取当前值）；⚠️ 须 CGC 追认 |
| D-1 | ~~`<DEV_ADDR>`~~ | ✅ **已定**（J-53 本人地址；v1 休眠 `perUseFee = 0`） |
| D-2 | developer 若误填为多签 ⇒ 收益隔离失效 | **硬约束：developer 必须 ≠ owner 多签** |

---

## §6 待裁定事项表

### 6.1 待 **J-25（你）** 裁定 / 确认

| # | 事项 | 本方案建议 | 影响 |
|---|---|---|---|
| **#7** | 子域名费用边界（1 WJ 归 DAO vs 运营商全自定） | **两笔钱两层**（§3.3） | 若改，约半天工作量 |
| **#10** | 留痕载体（GitHub Issue vs 链上） | **链上**（§3.2，非违背而是实现+升级） | 🔴 若不接受需返工 |
| **#11** | 起拍价 = 申请人首次出价 | 请补充意见 | 机制属新增 |
| **#12** | 加价幅度 / 5% 规则 | 请提供语雀原文或确认无 | 影响竞价实现 |
| **#13** | 拍卖时长 / 延时规则 | 请确认固定窗口 vs 加价重置 | 影响时间逻辑 |
| **#14** | 主域名二级流通是否放开 | 请表态 | 模块已实现，未开放 |
| **#16** | 加价是否重置倒计时 | 见 #13 | 影响时间逻辑 |
| **U-5** | `ClaimRegistry` 去留（当前零调用点、无资金） | 接上（工厂真调用）或删除 | 若不确，可当前保留不动 |
| **—** | `mintSubname` 是否需加 `to` 参数（运营商代会员铸造） | 请确认 | 若需，改动小 |

### 6.2 待 **J-53** 裁定

| # | 事项 | 现状 |
|---|---|---|
| U-1 | `<DEV_ADDR>` developer 地址 | ✅ **已定**（J-53 本人地址，见 §7.5.1；`perUseFee = 0` 休眠） |

### 6.3 待 **CGC** 决议

| # | 事项 | 现状 |
|---|---|---|
| U-2 | 二级市场是否开放 | 已实现并测试，未裁定开放 |
| U-3 | 提醒服务费归属 | 默认多签，待切 |

### 6.4 已定事项（**告知，无需裁定**）

| # | 事项 | 结论 |
|---|---|---|
| — | **mintFee 支付主体** | **根/父域名持有者**（§3.5） |
| — | 子域名 label | **纯数字**（禁前导零、≤19 位） |
| — | `maxDepth` | **5** |
| — | gas 优化 O-3/O-4 | **本轮不做**，留待你意见 |
| — | `renounceOwnership` | **全局禁用**（防绕道弃权） |

### 6.5 待定事项索引（U-1~U-9）

| # | 事项 | 裁定方 |
|---|---|---|
| U-1 | `<DEV_ADDR>` 未定 | J-53 |
| U-2 | 二级市场是否开放 | CGC |
| U-3 | 提醒服务费归属 | CGC |
| U-4 | R9-b 多签铸而不转 | J-53 / CGC |
| U-5 | `ClaimRegistry` 去留 | **J-25** |
| U-6 | 45 天超时未真实触发 | **J-25** |
| U-7 | ERC-677 回调真实链行为 | **J-25** |
| U-8 | `onTokenApproval` 文档订正 | **J-25** |
| U-9 | 文档历史数字残留 | J-53 |

---

## §7 部署方案（仅报备，不执行）

> **本节仅为方案报备。J-25 审查通过前不部署。**

### 7.1 脚本拆分（两部分，便于多签分步执行）

| 脚本 | 作用 | gas |
|---|---|---|
| `script/Deploy.s.sol`（Part 1） | **仅部署**（不含 setter） | **12,848,277** |
| `script/SetterCalldata.s.sol`（Part 2） | **仅产 calldata**（不广播） | 42,362 |

### 7.2 权限与执行者

| 项 | 内容 |
|---|---|
| 部署广播者 | **普通 EOA**（部署**不需要 owner 权限**） |
| constructor `owner` 参数 | **直接填多签 `0x4eF599b6E39D950D6Ddbd830fF5f95e06770C1B3`** |
| owner-only setter | 由**多签 2/3 自执行**（多签是合约无私钥，不能作 broadcast 发送者） |
| ⚠️ 常见误解更正 | `caller is not the owner` 的成因是**「调 setter 的地址」不是 owner**，**与谁部署无关** |

### 7.3 部署后必调 setter

| setter | 是否必须部署后调 |
|---|---|
| `setRegistrar`（每新建拍卖实例） | ✅ **必须** |
| 其余 5 项 | ❌ 已在 constructor / 默认值内联 |

> **每新建拍卖实例须 `registry.setRegistrar(auction, true)`。**

### 7.4 部署者 EOA（一次性）

| 项 | 内容 |
|---|---|
| 地址 | `0x17f5E1d9863E68034c531b9dFf2a6F61DF4EeB9f` |
| 当前余额 | **0 J**（实测） |
| nonce | **0** |
| 待充值 | **0.08 J**（部署 12.85M + 初始化 0.20M ≈ 0.0327 J + 余量） |
| 生命周期 | 部署完成后**即弃用**，不复用、不存资产 |
| 私钥 | 留存容器内（权限 600），**未回传、未打印、未写入任何文档** |

### 7.5 将来启用路径（提醒服务）

| 项 | 内容 |
|---|---|
| v1 状态 | **不启用**；`perUseFee = 0`（保持休眠） |
| developer | **已确认采用** J-53 本人地址 `0x8b4846d72d1530df755D9B5146A3e627a0A7147F`（**≠ 多签**，隔离不破） |
| 启用流程 | **J-53 本人自转让时启用**（`transferDeveloperRole`）；v1 休眠 |
| **预备用 developer 地址（J-53 反查结果）** | 见下 |

#### 7.5.1 J-53 地址反查结果

| 项 | 内容 |
|---|---|
| Core ID / 昵称 | `J53` / **微尘** |
| GitHub | `@fangsheng-sudo` |
| 个人域名 | `fangsheng.j` |
| 加入日期 | 2024/7/19 |
| **b32 地址** | `j33dyyd4edz5cd7a2andg5dglxy7s2w9rl8asa74` |
| **hex 地址（EIP-55）** | `0x8b4846d72d1530df755D9B5146A3e627a0A7147F` |
| **类型校验** | **EOA**（`eth_getCode` = `0x`）✅ |
| nonce | **276**（有链上活动） |
| 余额 | **0.406089800242515880 J** |
| **≠ 多签** | ✅ 与 `0x4eF599…C1B3` **不同** |
| **≠ 审核人 J-0** | ✅ 与 `0x5BF50F…076` 不同 |
| **≠ WJ / JNS** | ✅ 均不同 |
| **结论** | ✅ **已由 J-53 确认采用为 developer**（EOA、≠ 多签、校验和有效） |

> **出处**：`core-contributors.md` **L90**；b32→hex 用 `script/jvaddress.js`（Bech32m）；链上数据取自 JNS DAO 官方 RPC（授权端点，部署时配置）。
>
> **【J-53 已定·2026-09-15】采用方案 A**：部署时 `developer` **直接填** `0x8b4846d72d1530df755D9B5146A3e627a0A7147F`（J-53 本人地址），`perUseFee = 0` 保持休眠。
> 理由：**J-53 独自设置权立即成立**，且 **developer ≠ owner（多签）⇒ 权限隔离不破**。
> **状态**：**已确认采用**（不再是预备值）。

### 7.6 金丝雀试点与中止预案

| 文件 | 作用 |
|---|---|
| `金丝雀试点方案.md` | 小规模主网预演（约 0.0366 J） |
| `金丝雀试点执行手册.md` | 每步命令 + 执行者 + 断言方法 |
| `中止预案.md` | G0~G6 决策门；可修复重来 vs 不可逆 |
| `部署与多签执行清单.md` | 完整清单 |
| `多签执行calldata.txt` | A 段必执行 2 条 + B 段留痕 7 条 + C 段 developer |

---

## §8 附录

### 8.1 合约地址与常量

| 项 | 值 |
|---|---|
| **WJ** | `0x7fba9BB966189Db8C4fE33B7bf67Bfa24203c6AD`（**注意小写 c**） |
| **JNS** | `0xf8AbF36Bb2dc525b1E566d6B42F6Fd1BB2035b89` |
| **JNS DAO 多签** | `0x4eF599b6E39D950D6Ddbd830fF5f95e06770C1B3`（5860B 合约，3 owners，`required()`=2） |
| **审核人 J-0** | `0x5BF50F2931688F886F46f88D5CEEDE530bB92076`（**EOA**，`core-contributors.md` L37） |
| 主网 | chainId **3666**；RPC JNS DAO 官方 RPC（授权端点，部署时配置）；**无测试网** |
| gasPrice | 实测 **2.5 gwei**（`0x9502f907`） |

### 8.2 关键选择器（已与编译产物 `methodIdentifiers` 核对）

| 函数 | selector |
|---|---|
| `IJNS._nslookup(string)` | `0x54cf09e1`（缺键**返 0 不 revert**） |
| `ownerOf(uint256)` | `0x6352211e` |
| `owner()` | `0x8da5cb5b` |
| `transferAndCall` | `0x4000aea0`（字节码出现 2 次） |
| `approveAndCall` | `0xcae9ca51`（1 次） |
| `depositToAndCall` | `0x5ddb7d7e`（1 次） |
| `onTokenTransfer` | `0xa4c0ed36`（2 次） |
| `onTokenApproval` | `0x00ba451f` ⇒ **0 次、不存在** |

### 8.3 事件表（T3，全部含 域名/地址/金额/时间戳）

| 事件 | 字段 |
|---|---|
| `RequestSubmitted` | requestId, applicant, name, startingPrice, payloadHash, ts |
| `RequestApproved` | requestId, reviewer, approvals, ts |
| `RequestCancelled` / `RequestRejected` | requestId, actor, ts |
| `AuctionCreated` | requestId, auction, name, startingPrice, ts |
| `Bid` | auction, name, bidder, amount, newEndTime, ts |
| `Outbid` | auction, name, outbidder, refundAmount, ts |
| `RefundClaimed` | auction, user, amount, ts |
| `Settled` | auction, name, winner, amount, beneficiary, ts |
| `ApplicantWonInDefault` | auction, name, applicant, amount, ts |
| `MintRequested` | auction, name, winner, reviewRef, ts |
| `MintCompleted` | auction, name, winner, tokenId, ts |
| `WJReleasedToDAO` | auction, beneficiary, amount, ts |
| `TimeoutRefunded` | auction, name, winner, amount, ts |
| `EmergencyCancelled` | auction, name, reason, ts |
| `NameReservationReleasedByAuction` | name, requestId, auction, ts |
| `NameReservationReleasedByGov` | name, requestId, ts |
| `SubnameMinted` | parentId, subId, label, owner, depth, fee, ts |
| `TransferableSet` / `CanMintSubSet` | subId, flag, ts |
| `ParentControlRenounced` | subId, ts |
| `MaxDepthChanged` | oldD, newD, ts |
| `Sold` / `FeePaid` | market, tokenId, seller, winner, price, fee, ts |
| `BatchSettled` | batchId, user, count, amountWJ, periodStart, periodEnd, ts |
| `ChargeSkipped` / `ArrearsCapped` | batchId/user, amount/reason, arrears, ts |
| `MonthlyCapReached` | user, count, monthlyCharged, ts |
| `TradeFeeRateChanged` / `MintFeeChanged` / `BeneficiaryChanged` | old, new, ts |
| `PerUseFeeUpdated` / `FeeCapChanged` / `ServiceFeeRecipientChanged` | old, new, ts |
| `DeveloperTransferProposed` / `DeveloperTransferCancelled` / `DeveloperChanged` | candidate/old/new, ts |

### 8.4 合约行数

| 合约 | 行数 |
|---|---|
| `EnglishAuction.sol` | 334 |
| `JNSAuctionFactory.sol` | 323 |
| `SecondaryMarket.sol` | 246 |
| `ClaimRegistry.sol` | 153 |
| `SubnameRegistry.sol` | 234 |
| `NotificationService.sol` | 278 |
| `deps/Deps.sol` | 314 |

### 8.5 文件清单

| 用途 | 路径（相对本仓库根） |
|---|---|
| 主设计 | `设计说明.md`（49 KB） |
| 风险清单 | `风险清单.md` |
| gas 表（含口径更正） | `gas表.md` |
| 补测结果 | `补测结果.md` |
| 部署清单 | `部署与多签执行清单.md` |
| 多签 calldata | `多签执行calldata.txt` |
| 金丝雀方案 / 手册 | `金丝雀试点方案.md` / `金丝雀试点执行手册.md` |
| 中止预案 | `中止预案.md` |
| 源码 | `contracts/*.sol` + `contracts/deps/Deps.sol` |
| 测试 | `test/*.t.sol` + `test/mocks/*.sol` |
| 本材料 | `审查材料-J25.md` |

---

## §9 JEEP-7 原文四问应答

你在 `jips#7`「开放讨论」提出四问，逐条应答：

### 问 1：纯数字子域名是否足够？是否保留少数预留关键词？

**答**：
- 「足够」→ 从**免审查**目标看：**足够**。纯数字可由链上规则完全判定，无需人工。
- 从**可用性**看：代价是**可读性下降**（29n 亦提到此点）。我们已用 **长度 ≤19 + 禁前导零** 降低歧义。
- **预留关键词** → 纯数字后**不存在此问题**（全部数字串受同一规则约束，无字母关键词可预留）。

### 问 2：运营商是否可以自定义子域名续费价格？

**答**：
- 你的原始主张「子域名：是否收费、收多少…完全由运营商自行决定」在**会员侧**已完全尊重（DAO 不介入）。
- 但我们保留了**1 WJ 链上铸造费归 DAO**（对应链上存储/gas 成本）——**此点即 §3.3 待你裁定的 #7**。
- **若你要求「连 1 WJ 也由运营商定」⇒ 改动量约半天**（新增 `operatorFee` mapping + setter + 3~4 用例，不影响其他模块）。
- 另：v1 **无续费逻辑**（与你的「无 expiry」主张一致）；「续费价格」在本系统中**不适用**——铸造是一次性买断。

### 问 3：V3 的合约架构是否需要重构，还是在 V2 基础上升级？

**答**：
- 本系统**建立在 V2 JNS 之上**，且**不改 JNS 主合约**。
- 因 V2 **无 `setSubnodeOwner`**（`_nslookup` 是全串精确匹配、无层级），子域名必须**独立部署** `SubnameRegistry` —— 这是**被迫**，也是**省 gas**。
- **定位：V2 过渡方案**。若 V3 主合约实现层级（如你评论②描述的「单 Registry + `setSubnodeOwner`」），本模块可**退役或改造**。
- 你的评论①亦将「operator 独立部署子域名合约」列为**可选架构之一** ⇒ 本选择**非理念违背**。

### 问 4：拍卖机制是否一并调整？

**答**：
- **本材料即为该问题的提案。**
- 现状：官方 `jns-auction.md` 描述的是**人工夜拍专场**（晚 9 点、每场 5 个、主持人奖励）；你在 `workspace#72` 指出「**拍卖工作目前无人主持**」，并提出 GitHub Issue 7 天倒计时竞拍作为**退而求其次**。
- 本系统：把该流程**链上自动化**（无人主持、自动竞拍、自动结算），并把留痕载体**从 GitHub 升级为链上**。
- **待你裁示**：
  1. 是否接受「**链上自动拍卖**」作为「人工夜拍」的替代/并行方案？
  2. 是否需要保留「**每场 5 个**」「**主持人 1 元自由选**」「**预约排队**」等**官方场次机制**？（本系统为**独立实例**模式，**无场次/主持人/预约队列**概念）
  3. 时间规则用**固定 168h 硬顶**，还是改为你在 `#72` 提出的「**加价重置倒计时**」？

---

## 结语

| 项 | 状态 |
|---|---|
| 是否具备提交审查条件 | ✅ **具备**（140/140 测试通过；solc 0 告警；材料完整） |
| 是否具备部署条件 | ❌ **不具备**——等 J-25 审查通过 + 多签余额就位（`<DEV_ADDR>` ✅ 已定） |
| 最大待决 | 🔴 **#10 留痕载体**（若你不接受链上载体，需返工） |
| 最大残余风险 | 🔴 **R9-b**（多签铸而不转，赢家无链上救济）+ **X-1**（不可升级 + 无测试网） |

> **请重点回复 §6.1 的 #7 / #10 / #11~#14。** 其中 **#10 与 #13/#16 若与你的原意不符，可能需返工**，请在部署前明确。

---

*本材料全程只读生成，未 commit / push / 部署 / 广播。*
