# 风险清单（定稿）

> 生成：2026-09-14·**只读产出**｜对象：JNS 拍卖系统四批合约（EnglishAuction + JNSAuctionFactory / SecondaryMarket + ClaimRegistry / SubnameRegistry / NotificationService）
> 说明：本清单替代《交付说明.md》早期的 R-1~R-10 草案，为**全过程残余风险汇总**。等级：🔴 高 / 🟡 中 / 🟢 低。

---

## 一｜R9 多签相关（最高优先）

| # | 风险 | 等级 | 现状 / 处置 |
|---|---|---|---|
| **R9-a** | **多签在铸造前失效** ⇒ `JNS.claim` 仅 owner（多签）可调 ⇒ **域名铸不出** | 🔴 高 | 无链上解：45 天超时**全额退赢家**（出口②）兜底；**系统可用性依赖 2/3 多签存活**。已在试点断言与中止预案覆盖 |
| **R9-b** | **多签「铸而不转」**（claim 后未把 NFT 转给赢家） | 🔴 高 | `releaseToDAO` **软校验放行**（`ownerOf == JNS.owner()`）⇒ 款项照付多签，**赢家两头空**。❗**赢家无链上救济**，依赖多签诚信。（实测：`testS6_mintedToMultisigNotYetTransferred` 通过） |
| **R9-c** | 多签**铸给无关第三方** | 🟡 中 | `releaseToDAO` 白名单校验 **revert**（资金停 Held）→ 治理纠正后重触发；长期无法纠正走 `extendTimeoutWindow` + 特别决议 |

> **R9 结论（须写入风险公示）**：**多签铸而不转时赢家无链上救济，依赖多签诚信。**

---

## 二｜Mock 与真实链差异

| # | 差异 | 等级 | 影响 / 处置 |
|---|---|---|---|
| **M-1** | MockJNS **非真实 JNS 全量复刻**（仅 `_nslookup`/`ownerOf`/`claim`/CSBT）；真实 JNS 有 822 个既存 token、`_allTokensName`、`name2addr` 等 | 🟡 中 | 试点步骤 6 用**真实 JNS** 验证 `_nslookup != 0` 与 `ownerOf`（断言 #5） |
| **M-2** | Mock 用 `etch` 落到硬编码常量地址，**未跑构造函数**；`MockJNS.initialize()` 手工补 `nextId=1` | 🟢 低 | 仅测试装置问题；已在 Base 中固化 |
| **M-3** | **无测试网**（chainId 3666 无 testnet）⇒ 只能在本地 EVM 与**主网真跑**之间取舍 | 🔴 高 | 金丝雀试点即"主网小规模预演"；成本 ≈0.0366 J |
| **M-3b** | **正式部署 gas 预算**：主网无 testnet，演练只在本地 anvil；正式部署须 ① J-25 审查通过 ② 备足 gas | 🔴 高 | **测算（2026-09-16）**：部署 5 笔 gasUsed 合计 **13,615,988**；gasPrice **2.5 gwei**；基础成本 **0.03429 J**；×1.5~2 安全系数 ⇒ **0.0515~0.0686 J**；含部署后 2 条必调 setter。**当前 0.05 J 在 ×1 刚好、×1.5 起不足** ⇒ 建议备 **≥0.10 J** |
| **M-3c** | **部署前置清单**（正式部署前须逐项满足；任一未满足则不得部署） | 🔴 高 | ① **J-25 审查通过**（唯一解锁条件）<br>② **部署路径定死**：**建议 A（EOA 广播）** —— owner 已硬编码为多签（`Deploy.s.sol:59`）⇒ A/B 权限结果**一致**；A 无需新增脚本，B 须新增「产 deployment calldata」脚本 = **新增出错面**；**最终待 J-25 确认**<br>③ **owner 最终归属**：现设计 constructor 硬编码多签（`Deploy.s.sol:59` `owner = DAO_MULTISIG`）⇒ 已定；若改口径须同步改脚本<br>④ 若走 A：权限移交**必须当日完成并当场核验**——不可升级合约的 owner 一旦转错**不可逆**<br>⑤ **gas ≥0.10 J**（现 **0.11 J 已备** ✅）<br>⑥ **现场部署字节码 hash 须与审查通过时一致**（比对 `out/` 产物与审查基线）<br>⑦ **`JNSAuctionFactory` 尺寸复测**（余量仅 658 B） |
| **M-4** | 本地 gas ≠ 主网 gas（主网 gasPrice 2.5 gwei，实测） | 🟢 低 | 成本按主网实测价估算，留余量 |
| **M-5** | 真实 WJ 的 **`onTokenApproval` selector 实测不存在**（字节码 0 次），而 `WJ规则核查.md` 记载"有" | 🟡 中 | ⚠️ **文档与实测冲突，已如实标注**；本设计不使用该路径，不受影响；建议后续复核该文档条目 |
| **M-6** | `claim` 铸给多签后再转赢家需 **先 `unbind`**（CSBT 硬约束）；Mock 已复刻 | 🟢 低 | runbook 步骤 6 已写明两笔交易 |

---

## 三｜45 天超时未真实触发（未验证项）

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| **T-1** | **45 天超时退款**仅在本地 `vm.warp` 验证，**主网未真实经历 45 天** | 🟡 中 | 属时间依赖逻辑；本地已覆盖（`testS6_releaseBeforeMintReverts` 含 45 天后成功退款）。主网首次真实触发需等待；**建议届时专人复核** |
| **T-2** | `extendTimeoutWindow`（每次 ≤+45d，最多 3 次）**主网未真实触发** | 🟢 低 | 同上，本地通过 |
| **T-3** | 192h 封顶路径本地已验证（`testS3`，含 144 次连打触顶 + 越界防护） | 🟢 低 | 已 PASS |

---

## 四｜ERC-677 回调

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| **E-1** | 真实 WJ 含 `transferAndCall`/`approveAndCall`/`depositToAndCall`（字节码实测 ✓）⇒ 若有人用 AndCall 与拍卖合约交互 | 🟢 低 | 实测：拍卖合约**未实现钩子** ⇒ 整笔**原子回滚**、余额零变动（`testS2_transferAndCallToAuctionRevertsAtomically` PASS） |
| **E-2** | 回调内**重入/状态错乱** | 🟢 低 | 实测回调内嵌套 `withdrawRefund` 状态一致（`testS2_nestedCallDuringCallbackIsConsistent` PASS）；合约另用 `nonReentrant` |
| **E-3** | 本设计**内部一律只用 `transferFrom`** ⇒ 全程零回调 | 🟢 低 | `testS2_fullFlowNeverTriggersCallback` PASS |

---

## 五｜CGC 待决议（2 项）

| # | 待决议 | 等级 | 当前默认值 / 影响 |
|---|---|---|---|
| **C-1** | **提醒服务费 `serviceFeeRecipient` 归属**（开发运营方 vs JNS DAO） | 🟡 中 | 默认先设 **JNS DAO 多签**；**待 CGC 决议后**由 developer 改。改前/改后**不影响已结算历史** |
| **C-2** | **二级市场 `tradeFeeRate` 取法**（甲案：成交时取当前值 / 乙案：挂单时快照） | 🟡 中 | **已采纳甲案**（成交时取当前值，必须 emit + 建议提前公告）；⚠️ **须获 CGC 追认** |

> 另：`auctionBeneficiary`（一级拍卖受益）默认 = JNS DAO 多签；若 CGC 另有安排，可在**新建实例**生效（旧实例快照不可改）。

---

## 六｜DEV_ADDR 未定

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| **D-1** | `<DEV_ADDR>`（提醒服务实际开发运营方）**尚未确定** | 🟡 中 | 部署前必须替换；留 `address(0)` ⇒ `setServiceFeeRecipient`/`setPerUseFee` **无人可调** ⇒ **提醒服务不可用**（**其余四模块不受影响**，已实测 `test_devZero_otherModulesUnaffected` PASS） |
| **D-2** | developer 若**误填为多签** ⇒ **收益隔离失效**（owner 可绕道改费率） | 🟡 中 | 硬约束：**developer 必须 ≠ owner 多签**（脚本注释 + 清单 §4 明示） |

---

## 七｜其他残余风险

| # | 风险 | 等级 | 处置 |
|---|---|---|---|
| **X-1** | **合约不可升级** + 无测试网 ⇒ 主网首秀即定型 | 🔴 高 | 金丝雀试点前置（`金丝雀试点执行手册.md`）；中止预案分 G0~G6 决策门 |
| **X-2** | `settle()` **依赖外部调用**，无人调则资金/标的挂起 | 🟡 中 | 设计为**任何人可调**；可另配链下 bot 兜底 |
| **X-3** | ~~未铸造的同名可重复 submitRequest~~ ⇒ ✅ **已修（2026-09-15）** | — | **X-3 被重分类为【真漏洞】而非残余风险**（两个同名申请 → 两场拍卖 → 一方必被铸造占走，另一方 Held 资金因 `releaseToDAO` 恒 revert、旧退款又要求未铸造 ⇒ **永久锁死**）。已实修：① `submitRequest` 加 `require(requestOfName[name]==0, "FAC: name has live request")`（另有已铸造分支 `_nslookup==0`）；② 新增 `cancelRequest` + `rejectRequest` 释放 name 占用；③ `claimTimeoutRefund` 改**安全阀**（仅当 `releaseToDAO` 必 revert 时放行）。新增 6 用例全 PASS |
| **X-4** | 二级市场**挂拍即托管**（卖家须先自行 `unbind`，再 list 拉 NFT）= 链上必然两笔交易，存在**抢转窗口** | 🟡 中 | 已按设计（JNS 硬约束导致）；文档明示 |
| **X-5** | **申请人兜底中标**（无人加价时申请人以起拍价中标）是否合理 | 🟢 低 | 已按设计（起拍价 = 申请人首次出价）；行为已测（`testS5` PASS） |
| **X-6** | 手续费**取整方向**：二级 `floor`、拍卖 `ceil` | 🟢 低 | 已实现并测试（D-8） |
| **X-7** | `reviewRef` 仅作锚点，**合约不验证审核内容** | 🟢 低 | 按设计：审核在链下，链上只留哈希 |
| **X-8** | **Foundry lint `note` 181 条**（custom-errors / modifier-used-once 等风格建议） | 🟢 低 | **非编译器 warning**，不影响正确性；solc Warning = 0 |
| **X-9** | 多签**余额**需足够支付 gas | 🟢 低 | 实测 2026-09-14 22:15 = **0.320001 J** ≥ 0.05 J ✅ 充足 |
| **X-10** | **owner 紧急中止权**（`emergencyCancel`，`EA:304`；经 `JNSAuctionFactory.sol:260` 转发）：集中单一主体，可叫停未结算拍卖 | 🟡 中 | **已主动披露**（设计说明 §2.5「权限披露（主动）」）；仅适用未结算，中止后全额挂 pull 退款；与 R-4 同源 |
| **X-11** | **`JNSAuctionFactory` 字节码余量仅 658 B**（runtime 23,918 / EIP-170 上限 24,576） | 🟡 中 | 合约不可升级 ⇒ 今后**任何改动先算尺寸**（`forge build --sizes`）；触及上限须拆分或改参数 |
| **X-12** | **WJ 标准 ERC20 硬前提**（18 位整数、无转账税、无 rebasing、无回调）：记账假设到账额=请求额，无余额差校验 | 🟡 中 | **已标注**（README「⚠️ 声明」段「硬前提」条目，commit `ee271621`）；WJ 若变更特性须重新审计 |

---

## 八｜总评

| 维度 | 结论 |
|---|---|
| 合约正确性 | 🟢 本地 **140/140 测试通过**（13 suites）；solc 0 warning / 0 error / 0 Stack-too-deep |
| 最大残余风险 | 🔴 **R9-b（多签铸而不转，赢家无链上救济）** + **X-1（不可升级 + 无测试网）** |
| 待外部输入 | 🟡 CGC 两项决议（`<DEV_ADDR>` ✅ 已定） |
| **→ 是否具备试点条件** | **具备**（前置项已满足：余额充足、测试全过、参数内联明确）——但须**先补 `<DEV_ADDR>`**，且试点中一旦触及 `claim`/`releaseToDAO` 即不可逆 |
